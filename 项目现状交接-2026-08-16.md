# 多谱基座 · 项目现状交接文档（2026-08-16）

> 本文件由 2026-08-16 会话在 `E:\project\JiaPu_Web` 生成，供新会话快速接手。
> 请先读本文件 + `AGENTS.md` + `开发纪要.md` + `多谱基座改造-交接.md` 再开工。
> ⚠️ 2026-09-05 后新增：整理变动、照片压缩/留底、反爬方案 A、多多进宝 —— 见第六·B 节；全文以 `AGENTS.md` 为准。

---

## 一、项目一句话概况

**一套 Next.js 14 静态站承载多个家族宗谱**（多谱基座），当前已接入 **陈氏（chen）**、**赵氏（zhao）** 与 **王氏（wang）** 三家。配套 FastAPI 后端（认证/变更记录/反馈/照片），部署于华为云 ECS + Nginx，域名 https://chenmike.cn/chen。

---

## 二、当前已接入两谱状态

| 谱 | clanId | 人口 | 男女 | 世代数 | 房支 | 数据源素材位置 |
|---|---|---|---|---|---|---|
| 陈氏宗谱 | chen | 809 | 520男/289女 | 14 | 自龙/自凤/自虎/自秀 | `material/chen/` |
| 赵氏宗谱 | zhao | 415 | 342男/73女 | 14 | 世义/世湖/世玉/世龙 | `material/zhao/` |
| 王氏宗谱 | wang | 676 | 383男/293女 | 13 | 永昌/永泰/永吉/永庆（蒿塔塘王氏） | `material/wang/`（世系表.txt 已定稿，UTF-8） |

---

## 三、目录结构（2026-08-16 最新）

```
E:\project\JiaPu_Web\
├── material\                 ← 各家原始素材库（2026-08-16 从根目录移入，与代码分离）
│   ├── chen\                 世系表.txt（GBK母本）、整理稿、小程序二维码
│   ├── zhao\                 赵氏整理01.md（辈分版）、整理稿、PDF截图、.orig.bak繁转简备份
│   └── wang\                 王氏宗谱.pdf + 王氏宗谱-2018-4-21.docx（待整理，下一家）
├── src\
│   ├── app\                  page.tsx 谱系选择页；[clanId]\ 各谱 12 页 + layout.tsx（含 Navbar）
│   ├── components\           Navbar.tsx / Footer.tsx / TreeView.tsx / person\PhotoLightbox.tsx / ZmfButton.tsx
│   ├── lib\                  clans.ts（谱注册表）、data.ts（多谱数据层）、types.ts、validation.ts、
│   │                         api.ts（多谱 API 客户端）、router.ts（纯函数）、use-clan.ts（useClanId）
│   └── data\clans\{chen,zhao}\   各谱 meta.json + genealogy.json + documents.json + photo_map.json
├── backend\                  FastAPI 后端（main.py + db.py + auth.py + identity.py + image_utils.py + shixi_export.py
│   │                         + routers\{auth,changes,admin,feedback,zmf,photos,ddk}.py）
│   ├── data\clans\{chen,zhao,wang}\genealogy.json    ← 后端身份校验用的谱数据副本
│   └── uploads\{chen,zhao,wang}\    照片文件（C 方案全托管后端，系统命名 {姓名}.jpg / {姓名}O.jpg；
│                                    内含 prev\ 目录存换照留底旧图）
├── scripts\
│   ├── build_zhao_genealogy.py   解析 赵氏整理01.md → zhao/genealogy.json（含女性提取）
│   ├── build_wang_genealogy.py   解析 wang_mid.txt → wang/genealogy.json
│   ├── seed_photos.py            把旧 photo_map.json 照片迁移到 backend/uploads/ + photos 表（幂等）
│   ├── generate_people_data.py   按 clan 生成 people.json 到 public/data/clans/{clanId}/
│   └── export_changes_shixi.py   审核通过的 changes → 世系表.txt 风格（import 自 backend/shixi_export.py）
├── public\
│   ├── images\{chen,zhao,wang}\       各谱图片（chen 有 53 张照片 + logo + 小程序码）
│   └── data\clans\{chen,zhao,wang}\people.json   展平人物数据（客户端用）
├── 开发纪要.md、多谱基座改造-交接.md、AGENTS.md    ← 项目记忆（必读）
└── 网站连接备案\、用户手册.md 等
```

---

## 四、多谱架构关键点

### 4.1 谱注册机制（加新谱流程）
**加新谱 = 建数据目录 + clans.ts 加 import 和数组行，零代码改动其余部分**：
1. `src/data/clans/{clanId}/` 放 4 个 json（meta/genealogy/documents/photo_map）
2. `src/lib/clans.ts` 顶部加 4 行 import，`clans` 数组加 1 行
3. 复制 `genealogy.json` 到 `backend/data/clans/{clanId}/`
4. 运行 `python scripts/generate_people_data.py {clanId}` 生成 people.json

### 4.2 关键文件职责
- `src/lib/clans.ts`：谱注册表，集中 import 各谱 JSON，导出 getClanMeta/Genealogy/Documents/PhotoMap/getClanIds
- `src/lib/data.ts`：数据访问层。`flattenTree(clanId)` 展平树生成 `p_0,p_1,...` id；`branchGen`（陈氏=2，赵氏=3）分房支；`stripPrefix`（陈氏="陈"，赵氏=""）参数化
- `src/lib/router.ts`：纯函数（URL 推导 clanId、图片路径），**不能 import client 钩子**
- `src/lib/use-clan.ts`：`useClanId()` 必须 "use client"

### 4.3 赵氏特殊点（与陈氏差异）
- 赵氏字辈 = 辈分（第N世），谱内标注"血缘第N世"
- `generation` 覆盖字段：`PersonNode` 新增可选 `generation?: number`，用于承嗣跨辈（美均A=12世挂崇满A10世下、贤武A=13世挂济生11世下），flattenTree 用 `node.generation ?? gen`
- 女性节点从 detail 的"生女X："提取（73人），无名女儿用"赵长女/赵次女"占位，与陈氏"陈长女"一致

---

## 五、本次会话改动（2026-08-16）

1. **material/ 素材库**：各家素材从根目录移入 `material/{clanId}/`，脚本路径已同步
2. **赵氏繁转简**：`赵氏整理01.md` 已用 opencc 转简体（`乾`字特护，人名/乾隆/乾山巽向不转）；原文件备份为 `.orig.bak`
3. **赵氏数据修正**：重复节点 `钦孝`（德茂下）已删；`济倉`→`济仓A`（与济仓撞名区分）；女性节点 73 人已建
4. **根目录改名**：「我们的家谱」（原"多谱家谱/选择宗谱"），**Navbar 从根布局移到 `[clanId]/layout.tsx`**——根页面无标题栏，标题栏只在各谱内
5. **zhao氏→赵氏宗谱**：根 `page.tsx` 卡片名改用 `meta.name`（修掉 `zhao氏宗谱` 硬编码）
6. **小程序判断**：`[clanId]/page.tsx` 用 `fs.existsSync(public/images/{clanId}/miniprogram-code.png)` 判断有无小程序码，有则作为「功能导航」最后一张卡片，无则顺次递补
7. **微信公众号按谱区分**：`ClanMeta` 新增 `wechatAccounts?: string[]`；陈氏=`["陈家庄","我们的家谱"]`，赵氏=`["我们的家谱"]`；Footer 遍历渲染
8. **王氏宗谱接入完成**：676 人、13 世，构建脚本 `build_wang_genealogy.py`（解析 `wang_mid.txt` 中间稿，父先子后最近同名匹配，非从世系表解析）；`/wang` 全部 14 路由已静态导出
9. **公众号二维码弹窗**：`ClanMeta` 新增 `wechatQr?: Record<string,string>`（公众号→图片路径）；Footer 点击「微信公众号：xxx」弹出扫码弹窗；图片 `public/images/our-genealogy-qr.jpg`（取自 `material/我们的家谱_1280.jpg`）
10. **照片 C 方案（全托管后端）**：详见下方「照片模块」章节

---

## 五·B、照片模块（C 方案：全托管后端，2026-08-16 实现）

> 背景：族人上传照片需要系统命名规避重名/错名。设计为**前端静态站 + 后端照片托管**，上传即时生效、免审核、免重新发布。

- **存储**：`backend/uploads/{clanId}/{姓名}.jpg`（系统命名，本人 `{姓名}`、配偶 `{姓名}O`，姓名含 A/B/C 后缀由系统处理）
- **数据库**：`photos` 表（clan / person_id=人物规范名 / slot(self|spouse) / filename，UNIQUE(clan,person_id,slot)）
- **API**（`backend/routers/photos.py`）：
  - `GET /api/{clan}/photos` → 全谱 `{人物名:{self,spouse}}`（公开）
  - `POST /api/{clan}/photos`（multipart：person_name + self_file?/spouse_file?）→ 登录用户仅可传**自己**（姓名去后缀匹配），admin 可传任何人；校验图片类型 + ≤10MB（入口）；>1MB 后端自动压缩；upsert 覆盖
  - `DELETE /api/{clan}/photos?person_name=&slot=`（admin）
  - 照片静态访问：`/uploads/...`（main.py `StaticFiles` 挂载）
- **前端**：`person/page.tsx` 是 client 组件，useEffect 拉取全谱 photos map 建缓存（本页 self/spouse + 子女头像共用）；上传 UI 两槽位（本人/配偶）仿身份证传图；`src/lib/api.ts` 新增 `fetchPhotos/uploadPhoto/resolvePhotoUrl/deletePhoto`
- **旧照片迁移**：`python scripts/seed_photos.py`（幂等，chen 53 张已迁移，zhao/wang 无照片）
- **照片命名规则**：本人照片只写名字，配偶照片名字+O，无需 M/W；重名后缀由系统按 person 规范名自动加
- **上线注意事项**：**Nginx 必须加 `location ^~ /uploads/ { valid_referers none blocked server_names; proxy_pass http://127.0.0.1:8000; }`**（否则照片 404；⚠️ 必须 `^~` 前缀，否则被图片防盗链正则抢占）+ `client_max_body_size 10m;`；部署后在服务器跑一次 `seed_photos.py`（或把 `backend/uploads/` 一并上传）
- **照片自动压缩**（2026-09-05）：后端 `image_utils.compress_image`（Pillow），>1MB 原始图上传时自动转 JPEG 压缩到 ~900KB，用户无需手动压缩
- **换照自动留底**（2026-09-05）：上传更换时按 `photos` 表当前 filename 把旧图移入 `uploads/{clanId}/prev/` 并写 `photos_history` 表；`GET /api/{clanId}/photos/history?person_name=&slot=` 公开查历史

---

## 六、下一步工作

### P0：王氏宗谱接入（下一家谱）
- 素材：`material/wang/` 有 PDF（3.3MB）+ DOCX（8.4MB，2018年）
- 建议流程：提取文本 → 整理成类似 `赵氏整理01.md` 的树形 md（含辈分）→ 写 `build_wang_genealogy.py` 生成 json → 注册 clans.ts → 同步 backend → 生成 people.json → build 验证
- **注意**：参考赵氏的坑（字辈辈分确定、重名后缀、女性节点提取、承嗣跨辈 generation 覆盖）

### P1（开发纪要既有计划）
- D3.js 吊线图/世系图可视化；数据管理后台
- 数据库迁移（JSON → Prisma+SQLite）；CSV/PDF 导出
- 用户系统、留言板/修谱协作、全文检索
- 修谱日志/版本对比、广告接入（多多进宝已上线，见六·B）

---

## 六·B、2026-09-05 新增功能（整理变动 / 照片增强 / 反爬 / 多多进宝）

1. **整理人员变动**：管理后台「整理变动」页签一键把已审核变更渲染为世系表文本（不落库）。API：`GET /api/{clanId}/admin/export/pending`、`POST /export/generate`、`POST /export/mark`（写 `changes.exported_at` 防重复）。生成逻辑在 `backend/shixi_export.py`，`scripts/export_changes_shixi.py` 已改为 import 自它
2. **照片自动压缩**：`backend/image_utils.py`（Pillow），>1MB 自动转 JPEG 压到 ~900KB
3. **换照片自动留底**：旧图按 photos 表当前 filename 移入 `uploads/{clanId}/prev/` + 写 `photos_history` 表；`GET /api/{clanId}/photos/history` 公开查历史
4. **反爬方案 A（Nginx，生产生效）**：`/etc/nginx/conf.d/anti-crawl.conf`（http 级 limit_req general 10r/s、datadl 2r/s；map $bad_bot 黑名单、$bot_allow 白名单）+ `/etc/nginx/sites-available/chen-zongpu`（server 级：`if ($bad_bot=1) return 403`、图片防盗链 `valid_referers none blocked server_names;`、`location ~ ^/data/.../people\.json$` 限流）。⚠️ **`/uploads/` 必须 `location ^~ /uploads/`**（否则被图片正则抢占照片 404）。备份 `/root/*.bak_20260905`
5. **多多进宝 CPS**：后端 `backend/routers/ddk.py` + 前端 `/mall` 页 + Navbar「好物」入口；PID/凭证存 `backend/.env`（PDD_*）。详见 `docs/多多进宝接入记录.md`
6. **Git 仓库**：已推 GitHub `ike333/jiapu-web`（首次推送被 Push Protection 拦截 token，已清除；`.gitignore` 排除 backend/.env、db、uploads、venv、备案截图）

---

## 七、常见坑（务必记住）

1. **`.next` 缓存损坏**：`Cannot find module './845.js'` / `'./vendor-chunks/next.js'`——**build 与 dev 不能同时开**（共用 .next 互相覆盖）。修复：停 node → 删 `.next` → 重建
2. **PostCSS 白屏**：`globals.css` 不能用 `@import url()`；Google Fonts 用 `layout.tsx` 的 `<link>`
3. **postcss.config.js / tailwind.config.js 必须 .js**（CommonJS），不能用 .ts
4. **useParams 必须放 `src/lib/use-clan.ts`**（"use client"）；`router.ts` 保持纯函数，否则 server 组件编译失败
5. **FastAPI authorization 参数**必须声明为 `Header(None)`，否则被当 query 参数导致 401
6. **赵氏繁转简用 opencc**（`pip install opencc-python-reimplemented`），注意"乾"字保护
7. **世系表.txt 为 GBK 编码、只读属性**；export_changes_shixi.py --apply 写母本前需先取消只读
8. **Nginx `valid_referers` 只能写关键字 `server_names`**，切勿展开成 `*.chenmike.cn chenmike.cn`（报 conflicting parameter）
9. **`/uploads/` 必须 `location ^~ /uploads/`**：普通 `location /uploads/` 会被图片防盗链正则 location 抢占 → 照片 404
10. **解压后权限修正**：`find /var/www/chen-zongpu -type d -exec chmod 755 {} +` + `-type f -exec chmod 644 {} +`，否则 `_next/static` 缺执行位 → 静态资源 404

---

## 八、常用命令

```powershell
npm run dev                    # 开发 → http://localhost:3000（/chen、/zhao）
npm run build                  # 生产构建 → out/（先停 dev，避免 .next 冲突）
npx serve out/                 # 本地预览静态产物
cd backend; python -m uvicorn main:app --host 0.0.0.0 --port 8000   # 启动后端
python scripts/generate_people_data.py zhao    # 生成 people.json（chen/zhao/wang）
python scripts/build_zhao_genealogy.py         # 赵氏整理01.md → genealogy.json
python scripts/build_wang_genealogy.py         # wang_mid.txt → wang/genealogy.json
python scripts/seed_photos.py                  # 旧 photo_map 照片 → backend/uploads/ + photos 表（幂等）
python scripts/export_changes_shixi.py --apply # 变更合并进世系表母本
```

## 九、部署流程（详见开发纪要.md 第九章）
1. 停 dev → `npm run build` → 用 .NET ZipArchive 正斜杠打包 out.zip（勿用 Compress-Archive）
2. 华为云 CloudShell / scp 上传 → 解压到 `/var/www/chen-zongpu/`（先 `rm -rf *`）+ 修正权限（755/644）
3. `curl -sI http://chenmike.cn/_next/static/css/xxx.css` 应为 200
4. 认证模块上线：服务器装 Python3+FastAPI，Nginx 加 `location /api/ { proxy_pass http://127.0.0.1:8000; }`
5. **照片模块（C 方案）**：Nginx 加 `location ^~ /uploads/ { valid_referers ...; proxy_pass http://127.0.0.1:8000; }` + `client_max_body_size 10m;`；把 `backend/uploads/` 随代码上传或服务器跑 `seed_photos.py`
6. **⚠️ 生产 build 前必须置空 API_BASE**：`.env.local` 的 `NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000` 会被内联进 JS 产物，若带着 build 则用户浏览器会访问自己电脑的 127.0.0.1（认证/照片全挂）。生产构建时临时删掉/注释 `.env.local`（`NEXT_PUBLIC_API_BASE=""` 走同源反代），本地 dev 再恢复
7. **反爬**：`/etc/nginx/conf.d/anti-crawl.conf` + `chen-zongpu` server 级配置（见六·B / 华为云部署指南）