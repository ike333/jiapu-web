# 管理员手册

> 适用范围：多谱家谱网站（多谱基座，chenmike.cn）。本手册把管理员日常会用到的功能、操作流程与注意事项汇总在一起，面向谱系管理员。

---

## 一、管理员能做什么

管理员（users 表中 `role=admin` 的用户）比普通注册用户多出以下能力：

- 进入**管理后台**：审核族人信息变更（新生/结婚/去世）、回复用户反馈
- **照片管理**：可代替任何族人上传/更换本人及配偶照片（普通用户只能传自己）
- **数据落地**：审核通过的变更，可导出为世系表格式并合并进族谱母本

普通用户只能：浏览、注册登录、提交信息变更、提交反馈、上传自己的照片。

---

## 二、管理员登录

管理入口统一为密码登录（不需要手机号注册）：

1. 打开 `/chen/login`（或对应谱的登录页）
2. 切换到「管理员」标签页
3. 输入管理员密码，点击登录

- 默认密码：`admin123456`（**生产环境必须修改**，见下文「环境变量」）
- 登录后返回的管理员身份为虚拟用户 `admin`，不占 users 表
- 管理后台地址：`/{clanId}/admin`
- 管理员的 JWT token 带谱声明，**跨谱访问会返回 401**（如用 chen 的 token 访问 zhao 后台）

> 前端如何确认自己是不是管理员：登录页管理员模式调 `adminLogin`，token 正常下发即通过；非管理员访问 `/admin` 会被自动重定向回 `/submit`。

---

## 三、管理后台：变更审核

进入 `/{clanId}/admin` 后默认显示「变更审核」板块。

### 三种变更类型

| 类型 | 图标 | 展示内容 |
|---|---|---|
| 新生（birth） | 👶 | 新生儿姓名、性别、出生日期/时间、父亲、母亲、出生地 |
| 结婚（marriage） | ❤️ | 新郎、新娘、新娘出生日期、结婚日期 |
| 去世（death） | ✝️ | 逝者姓名、去世日期、享年、安葬地 |

### 审核操作

- **待审核**：列表显示所有 `pending` 状态记录，每条卡片下方有「通过」/「驳回」两个按钮
- **通过**：记录状态变为 `approved`，自动记录审核人与审核时间
- **驳回**：记录状态变为 `rejected`，自动记录审核人与审核时间
- 审核完成后记录移到「已通过」/「已驳回」标签页，可通过顶部的待审核/已通过/已驳回三个标签切换查看

> 注意：审核**只改变状态，不会自动写入族谱数据**。通过后的变更要真正入谱，需管理员手动跑导出脚本（见第五章）。

### 与手机端小程序的差异

三端（Web/App/小程序）共用同一套 changes 数据，后台审核对三端生效。

---

## 四、管理后台：反馈管理

在管理后台顶部切换到「反馈管理」板块：

- 标签：待回复 / 已回复 / 全部
- 每条反馈显示：提交人姓名、手机号、时间、反馈内容
- 对「待回复」的反馈，在文本框中填写回复内容并点击「回复」即可
- 回复后记录状态变为 `replied`，自动记录回复人与回复时间

---

## 五、审核通过的变更 → 写入族谱

审核通过 ≠ 入谱。管理后台新增了「**整理变动**」页签，**无需命令行**即可把审核通过的变更一键整理成世系表文本：

### 管理后台「整理变动」页签（推荐）

1. 进入 `/{clanId}/admin`，切换到「整理变动」页签
2. 页签里显示当前**未整理**的记录条数（可展开查看每条待整理明细）
3. 点击「生成世系表条目」→ 后台把已审核记录渲染为世系表风格文本（含世代区块标题、育子/育女追加、新增行等），可直接复制或下载 `.txt` 保存到 `material/{clanId}/`
4. 人工**核对后并入族谱母本**（世系表.txt + genealogy.json，见第六章）
5. 核对完毕点「**标记已处理**」→ 记录写入 `exported_at` 时间，之后不再出现在待整理列表（防重复导出）

行为说明：

- **新生（birth）**：自动在父行的「育子/育女」列表追加新名，并插入新行到对应世代区块末尾
- **结婚 / 去世**：只生成提示文本，**需人工核对后补录**（可能信息不全）
- 文件仅在「生成」时渲染，**不落库、不写母本**；「标记已处理」才写入 `changes.exported_at` 时间戳
- 世系表.txt 为 GBK 编码，人名省略姓前缀（如"陈"）；生成预览可直接核对

### 命令行方式（备用）

生成逻辑与后台页签共用 `backend/shixi_export.py`（预览时不改母本）：

```bash
python scripts/export_changes_shixi.py         # 生成条目到 material/chen/世系表-新增条目.txt
python scripts/export_changes_shixi.py --apply # 直接合并进世系表.txt（自动备份 .bak）
```

> 落地后别忘了把更新同步到族谱数据（见第六章）。

---

## 六、照片管理

### 存储与命名

- 照片存后端 `backend/uploads/{clanId}/` 目录
- 命名规范：本人照片 `{姓名}.jpg`，配偶照片 `{姓名}O.jpg`（姓名含 A/B/C 后缀时保留）
- 数据库 `photos` 表记录归属（clan / person_id / slot / filename）
- 照片静态访问地址：`/uploads/{clanId}/{文件名}`
- 支持 jpg/png/webp/gif，单张不超过 **10MB**（前端入口上限；后端对 >1MB 的照片自动用 Pillow 压缩到 ~900KB）

### 上传 / 更换（人物详情页）

1. 打开某人的人物详情页（如 `/chen/person?id=p_xx`）
2. 登录后点「上传照片」/「更换照片」按钮
3. 选择「本人照片」和/或「妻/夫照片」，点「上传」
4. 上传即生效，无需重新发布网站

权限规则：

- **管理员**：可给任意族人上传/更换
- **普通用户**：只能传自己（去后缀后须与注册姓名一致），给他人传会提示"需要管理员代传"
- 重复上传同槽位自动覆盖（upsert）

### 换照片自动留底（2026-09-05 新增）

上传更换照片时，**旧照片不会丢失**：

- 若该 `person+slot` 已有旧图，系统按 `photos` 表当前 filename 把旧图移入 `backend/uploads/{clanId}/prev/`，并写入 `photos_history` 表
- 保留历史版本可随时追溯，防止误覆盖
- 旧图按**当前文件名**抓取（注意后缀 .jpg/.jpeg 可能不同），不以新上传文件名为准
- 公开查询接口：`GET /api/{clanId}/photos/history?person_name=规范名&slot=self|spouse`

### 删除

目前前端未提供删除按钮，管理员需通过 API 删除：

```
DELETE /api/{clanId}/photos?person_name=规范名&slot=self|spouse
```

（需带 Bearer 管理员 token；会同时删除磁盘文件与数据库记录）

### 旧照片迁移

首次上线或补录旧照片用（幂等，可重复执行）：

```bash
python scripts/seed_photos.py          # 全部谱
python scripts/seed_photos.py chen     # 只处理指定谱
```

### 生产上线注意事项

- **Nginx 必须加**：`location ^~ /uploads/ { valid_referers none blocked server_names; proxy_pass http://127.0.0.1:8000; }`，否则照片 404；且在 `server{}` 内加 `client_max_body_size 10m;`，否则 >1MB 照片上传报 413
  - ⚠️ **必须用 `^~` 前缀匹配**（不能只写 `location /uploads/`），否则会被 Nginx 图片防盗链的正则 location 抢占，照片返回 404
- 上传接口会自动压缩照片到 1MB 内（Pillow，转 JPEG），用户无需手动压缩图片
- 服务器需要 `backend/uploads/` 目录或部署后跑一次 `seed_photos.py`

---

## 七、族谱数据更新流程

族谱母本变化后（含审核入谱），前端/后端数据都要同步：

1. 更新 `src/data/clans/{clanId}/genealogy.json`（母本）
2. **复制一份到 `backend/data/clans/{clanId}/genealogy.json`**（注册身份校验读的是这份副本）
3. 重新生成前端展平数据：
   ```bash
   python scripts/generate_people_data.py chen   # 或全部谱
   ```
   （生成 `public/data/clans/{clanId}/people.json`）
4. 重新构建部署前端（见第八章）

---

## 八、构建与部署

### 本地开发

```bash
npm run dev                   # 前端 → http://localhost:3000
cd backend
python -m uvicorn main:app --host 0.0.0.0 --port 8000   # 后端 API
```

- 开发环境前端通过 `.env.local` 的 `NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000` 访问后端
- **dev 与 build 共用 .next 目录，跑 build 前先停 dev**，否则线上页面请求旧 chunk 会 404

### 生产构建与发布

```bash
npm run build                 # 静态导出 → out/
# 打包 out.zip → 华为云 CloudShell 上传 → 解压到 /var/www/chen-zongpu/
```

> **⚠️ 生产 build 前必须置空 API_BASE**：`.env.local` 中的 `NEXT_PUBLIC_API_BASE` 会被内联进 JS 产物。若带着 `127.0.0.1` build，用户浏览器会请求自己电脑的地址，认证/照片全挂。生产构建时临时置空（走 Nginx 同源反代），本地 dev 再恢复。

### Nginx 反代（认证/照片必需）

```nginx
# 照片上传需放开请求体上限（默认 1MB 会拦截大图；后端会自动压缩到 1MB 内，这里留足原始图余量）
client_max_body_size 10m;

location /api/      { proxy_pass http://127.0.0.1:8000; }
location ^~ /uploads/  { valid_referers none blocked server_names; proxy_pass http://127.0.0.1:8000; }
```

> `client_max_body_size 10m` 必须加在 `server {}` 块内。后端上传接口接收原始图后会自动用 Pillow 压缩到 1MB 以内，用户无需自己压缩图片。若不加，超过 1MB 的照片会被 Nginx 以 413 拦截（上传失败 (413)）。
> **`^~` 前缀必须写**——否则 `/uploads/` 会被下面的图片防盗链正则 location 抢占，照片 404。完整反爬配置见《华为云部署指南》「反爬方案 A」。

### 验证

```bash
curl -sI https://chenmike.cn/_next/static/css/xxx.css   # 应返回 200
```

---

## 九、环境变量与安全

后端读取的环境变量（`backend/` 目录下 `.env` 或系统环境）：

| 变量 | 默认值 | 说明 |
|---|---|---|
| `ADMIN_PASSWORD` | `admin123456` | 管理员密码。**生产必改**，否则任何人可进后台 |
| `JWT_SECRET` | （内置） | JWT 签名密钥，生产应设为随机长字符串 |
| `SMS_ENABLED` | `0` | 短信验证码开关；`0` 为模拟模式（接口直接返回 dev_code） |
| `SHIXI_FILE` | `material/chen/世系表.txt` | 世系表母本路径（导出脚本用） |

安全要点：

- 管理员密码别用默认值
- 不要把 `JWT_SECRET` 等写进代码仓库
- 手机验证码目前是模拟的，接入真实短信服务商后需改 `backend/auth.py` 的 `SMS_ENABLED=1` 分支

---

## 十、数据库速查

SQLite 数据库：`backend/data/genealogy.db`（单文件，备份直接复制该文件）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `users` | 注册用户 | phone、name、role(user/admin)、**clan**（手机号跨谱唯一） |
| `sms_codes` | 短信验证码 | phone、code、expires_at |
| `changes` | 族人信息变更 | type(birth/marriage/death)、data(JSON)、status(pending/approved/rejected)、**exported_at**（整理变动标记时间）、**clan** |
| `feedbacks` | 意见反馈 | content、status(pending/replied)、reply、**clan** |
| `photos` | 照片记录 | person_id、slot(self/spouse)、filename、**clan** |
| `photos_history` | 换照留底 | person_name、slot、old_filename、created_at（照片 C 方案，2026-09-05） |

---

## 十一、常见问题排查

| 现象 | 原因 / 处理 |
|---|---|
| 后台提示"需要管理员权限" | 用的不是管理员账号，请走登录页「管理员」标签密码登录 |
| 访问 API 返回 401 | token 谱不匹配：chen 的 token 只能访问 chen 的 API |
| 上传提示"族谱中不存在「xxx」" | 姓名必须是族谱规范名（含 A/B/C 后缀），普通用户还要与自己注册姓名一致 |
| 上传提示"文件过大" | 入口上限 10MB；>1MB 后端会自动压缩，仅超大原始图会被拒，或让管理员处理 |
| 照片线上 404 | Nginx 缺 `/uploads/` 反代（且必须用 `location ^~ /uploads/`），或服务器缺 uploads 目录 |
| 照片改了但手机端看不到 | 照片存后端即取即用，无需重发布；清一下手机端缓存 |
| 审核通过后族谱没变化 | 正常——审核不自动入谱，需在管理后台「整理变动」生成条目并入母本 |
| 登录页收不到验证码 | 当前为模拟验证码，`send_sms_code` 会返回 dev_code 自动填入 |
