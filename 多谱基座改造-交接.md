# 多谱基座改造 · 交接文档

> 本文件由 2026-08-15 在 `E:\project\Zhao_Web` 会话中生成。新会话（工作目录 `E:\project\JiaPu_Web`）请先读本文件 + `AGENTS.md` + `开发纪要.md` 再开工。
> 当前状态（2026-09-05 更新）：**阶段一～四全部完成**——前端多谱化、后端多谱化、陈氏迁入、赵氏（415 人）+ 王氏（676 人）均已接入 `/zhao`、`/wang`。此后新增：整理人员变动、照片压缩/留底、反爬方案 A、多多进宝（详见 `网站现状.md`、`开发纪要.md`）。
> 路径迁移（2026-08-16）：各家原始素材已统一移入 `material/{clanId}/`（陈氏 `material/chen/`、赵氏 `material/zhao/`，含 zhao_pdf_pages/）。`scripts/build_zhao_genealogy.py` 输入为 `material/zhao/赵氏整理01.md`；`scripts/export_changes_shixi.py` 读写 `material/chen/世系表.txt`。文内所引根目录旧路径均已作废。

---

## 一、项目背景与目标

- 陈氏宗谱（父亲/爷爷支）已上线：`https://chenmike.cn/chen`，Next.js 14 + 静态导出 + FastAPI 后端，809 人/14 世/四大房支，照片 53 张。
- 赵氏宗谱（妈妈/外公支）待做，数据源 `E:\project\Zhao_Web\赵氏宗谱.md`（75 页 OCR 整理，已确认是全部，人物约 190+）。
- 未来还要做妻子一支、其他亲戚朋友宗谱。
- **目标**：一套程序，各支数据在各自子目录，加新谱零代码改动。杜绝"每支复制一套程序"的同步麻烦。

## 二、已定决策

| 决策点 | 结论 |
|---|---|
| 架构 | 一套程序 + `src/data/clans/{clanId}/` 数据子目录 + `app/[clanId]/` 动态路由 |
| 基座目录 | 新建统一目录（即本目录 `E:\project\JiaPu_Web`） |
| 后端 | 单进程多谱：DB 的 changes/feedback 表加 `clan` 字段，API 路径带前缀 `/api/chen/...`、`/api/zhao/...` |
| 实施顺序 | 先陈氏迁入基座并上线验证无回归，再做赵氏 |
| 重名处理 | 沿用陈氏 A/B/C 后缀惯例；赵氏错录条目先标记清单再与用户确认 |

## 三、多谱改造技术要点

### 1. 路由重构（前端）
- `src/app/` 下 7 路由页（/, family-tree, person, search, stats, documents, about）+ 5 功能页（login, submit, my-changes, admin, feedback）全部迁入 `src/app/[clanId]/...`
- 根 `page.tsx` 改为谱系选择页；`[clanId]/layout.tsx` 用 `generateStaticParams` 列出全部 clanId，构建时全量导出
- URL 保持不变：`chenmike.cn/chen/...`、`chenmike.cn/zhao/...`（clanId 即 URL 段）
- `next.config.js` 去掉 basePath `/chen`（原陈氏站依赖 basePath，迁移时注意链接生成）

### 2. 数据子目录化
```
src/data/clans/
  chen/  genealogy.json + documents.json + photo_map.json + meta.json
  zhao/  genealogy.json + documents.json + photo_map.json + meta.json
```
- `meta.json` 字段（草案）：
  ```json
  {
    "clanId": "chen",
    "name": "陈氏宗谱",
    "surname": "陈",
    "rootName": "陈三进",
    "branches": ["自龙", "自凤", "自虎", "自秀"],
    "branchGen": 2,
    "stripPrefix": "陈",
    "themeColor": "#1565C0",
    "features": { "auth": true, "submit": true, "feedback": true }
  }
  ```
- `src/lib/data.ts` 需改造的硬编码点：
  - `flattenTree()` 模块级缓存 `_flatCache`/`_nameIndex` → 改为 `Map<clanId, cache>` 隔离
  - `flattenTree()` 内 `gen === 2 ? node.name.replace(/^陈/, "")`（第 44 行）→ 按 meta 的 branchGen/stripPrefix 参数化
  - `getSummary()` 的 `branches: ["自龙","自凤","自虎","自秀"]`（第 291 行）→ 读 meta
  - `getTreeRoot()` 取 `genealogyData[0]` → 读 meta.rootName
- 附件路径：`public/images/{clanId}/`、`public/data/clans/{clanId}/people.json`、photo_map 路径带 clanId
- `scripts/generate_people_data.py` 按 clan 分别生成

### 3. 后端多谱化（单进程）
- `backend/` 复制到本目录；`identity.py` 按 clanId 读 `backend/data/clans/{clanId}/genealogy.json`
- `changes`/`feedback` 表加 `clan` 字段；API 前缀 `/api/{clanId}/...`
- 前端 `src/lib/api.ts` 按当前谱构造 base；Nginx `location /api/` 统一反代单后端

### 4. 赵氏数据注意点
- 赵氏第 4 代字辈恰为"赵"（赵文/赵武），名字**不加姓前缀**，branchGen=3（第 3 代世义/世湖/世玉/世龙分房支），与陈氏不同
- 赵氏 `赵氏宗谱.md` 世代标题 29 个且顺序混乱（【一世】→【十世】后跳回【六世】），需按分支重排
- 重名 10 处：必林、德海、士俊、士杰、美均、济乾、崇永、崇云、济根、济均
- 错录待确认清单（约 10 处）：114 行"崇永…生子一：崇永"、159 行"生子二：钦江、钦海"、143 行"崇保（钦勇次子）缺长子"、济生归属不明等
- 赵氏世系表.txt 用"育子/育女"措辞以兼容 `scripts/export_changes_shixi.py` 正则（`育子[一二…]：...`），GBK 编码

## 四、当前版本陈氏原站备忘

- 陈氏谱：始迁祖陈三进，四大房支自龙/自凤/自虎/自秀，809 人、14 世（世系表 14 代标题：三/自/文/陈/世/廷/山/启/建/章/安/仁/崇/道）
- 上线验证清单：首页统计 809/14、四大房支、搜索、53 张照片、文档 5 篇、登录注册（身份校验 genealogy.json 比对）、提交变更、管理员审核、反馈
- 迁移到新站后必须重新 `npm install`（npmmirror 镜像在 .npmrc）
- 部署：华为云 ECS 1.94.222.136 + Nginx，`chenmike.cn`；新架构整站 out 挂网站根，保留 `/chen` URL
- git push/fetch 前必须先查 WATT Toolkit（`Get-Process | Where-Object { $_.ProcessName -match 'watt|steampp' }`）

## 五、已完成实施记录（2026-08-15）

### 阶段一 · 前端多谱化（已完成，build 通过）
- 数据迁移：`src/data/clans/chen/`（genealogy.json / documents.json / photo_map.json / meta.json）
- `src/lib/clans.ts`：谱注册表，集中 import 各谱 JSON；新谱 = 数据目录 + 此文件加 import 和数组行
- `src/lib/data.ts`：重写为多谱版，所有公开函数首参 `clanId`；缓存按谱隔离；branchGen/stripPrefix 参数化
- `src/lib/router.ts`：**纯函数**（getClanIdFromPath/getImageBase/makeImagePath），不导入任何 client 钩子
- `src/lib/use-clan.ts`：**"use client"** 专属文件，`useClanId()`（useParams 取 [clanId]）
  - ⚠️ 关键坑：`useParams` 必须在独立 "use client" 文件，否则 server 组件（如 `[clanId]/page.tsx`）import router.ts 会编译失败
- `src/lib/api.ts`：重写，`setActiveClan`、token 按谱隔离（key=`${clanId}_genealogy_token`）、所有函数带 clanId，路径 `/api/{clanId}/...`
- 路由：全部 12 页迁入 `src/app/[clanId]/`；根 `page.tsx` 谱系选择页；`[clanId]/layout.tsx` generateStaticParams
- `next.config.js`：已删 basePath（`output:'export'` 保留）
- 图片：全部移至 `public/images/chen/`（55 文件）；`scripts/generate_people_data.py` 改为按 clan 生成到 `public/data/clans/{clanId}/people.json`（陈氏 809 人已生成）
- 陈氏 build 无回归：URL `/chen/...`，17 个静态页全导出

### 阶段二 · 后端多谱化（已完成，冒烟测试通过）
- `db.py`：users 表重建为 `UNIQUE(phone,clan)`，changes/feedbacks 加 `clan` 列；`_migrate()` 自动迁移旧库（旧数据归 'chen'）
- `auth.py`：token 带 `clan` 声明（create_token 加 clan 参数）；`token_clan()` 取谱；`resolve_user/get_user/find_user_by_phone` 按谱过滤
- `identity.py`：按 clanId 读 `backend/data/clans/{clanId}/genealogy.json`（`backend/data/clans/chen/` 已建并复制）
- `routers/*`：所有 prefix 改 `/api/{clan_id}/...`；各 handler 首参 `clan_id`；跨谱 token 校验（token_clan 不匹配 → 401）
- **补全 `feedback.py`**：原为 0 字节空文件（导致后端启动失败），已按前端 api.ts 实现 submit/mine/list/reply 四接口
- 冒烟测试通过：send-code→register(身份校验)→submit birth→admin-login→pending→review→feedback→reply 全链路；跨谱 token 隔离生效

## 六、下一步（在新会话执行）

1. 读 `AGENTS.md` + `开发纪要.md` + 本文件，确认理解
2. 阶段一：前端路由重构 + 数据子目录化 + meta.json
3. 阶段二：后端多谱化
4. 阶段三：陈氏数据迁入 clans/chen/ → build → 本地验证 → 部署验证无回归
5. 阶段四：赵氏数据整理（md → 世系表.txt → genealogy.json）→ 接入 /zhao

> 依赖安装后如需验证，开发：`npm run dev`；构建：`npm run build`；本地预览：`npx serve out/`