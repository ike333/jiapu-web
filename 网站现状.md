# 网站现状（2026-09-05）

> 多谱家谱网站 JiaPu_Web 当前基本状况速览。适用于新会话快速了解项目状态。
> 详细技术档案见 `开发纪要.md`；改造过程见 `多谱基座改造-交接.md`。

## 一、一句话概括

一套 Next.js 14 程序承载多个宗谱的"多谱基座"网站：已上线 **陈氏（`/chen`）、赵氏（`/zhao`）、王氏（`/wang`）** 三谱；静态导出 + 独立 FastAPI 后端（认证/变更记录/反馈/照片/导购）。配 Nginx 反爬方案 A（限流 + UA 黑名单 + 防盗链）已于生产生效。

## 二、项目进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段一 | 前端多谱化（路由重构 + 数据子目录化） | ✅ 完成，build 通过 |
| 阶段二 | 后端多谱化（单进程多谱 + 补全 feedback.py） | ✅ 完成，冒烟测试通过 |
| 阶段三 | 陈氏迁入基座，验证无回归 | ✅ 完成（`/chen/...` URL 保持） |
| 阶段四 | 赵氏数据整理并接入 `/zhao` | ✅ 完成（415 人、14 世） |
| 阶段五 | 王氏数据接入 `/wang` | ✅ 完成（676 人、13 世，2026-08-16） |
| 阶段六 | 反爬方案 A + 整理变动 + 照片留底/压缩 + 多多进宝 | ✅ 完成（2026-09-05，生产已生效） |

## 三、线上状况

- 域名：`https://chenmike.cn/chen`（陈氏）、`/zhao`（赵氏）、`/wang`（王氏）
- 部署：华为云 ECS（1.94.222.136）+ Nginx，静态导出整站 out 挂网站根
- 后端：FastAPI（uvicorn :8000，systemd `backend-jiapu.service`），Nginx 反代 `/api` → 8000
- 反爬：`/etc/nginx/conf.d/anti-crawl.conf`（http 级限流 + UA 黑/白名单）+ `chen-zongpu`（server 级防盗链/people.json 限流）

## 四、当前数据结构

| 谱系 | clanId | 规模 | 状态 |
|---|---|---|---|
| 陈氏宗谱-棕溪 | `chen` | 809 人 / 14 世 / 四大房支（自龙/自凤/自虎/自秀） | ✅ 已接入 |
| 赵氏宗谱 | `zhao` | 415 人 / 14 世 | ✅ 已接入 |
| 王氏宗谱 | `wang` | 676 人 / 13 世 | ✅ 已接入 |

- 各谱数据：`src/data/clans/{clanId}/`（genealogy/documents/photo_map/meta.json）；照片在 `public/images/{clanId}/`；people.json 在 `public/data/clans/{clanId}/`（脚本生成）
- 后端数据副本：`backend/data/clans/{clanId}/genealogy.json`（注册身份校验用，更新数据后需重新复制）

## 五、功能清单（已实现）

- 浏览：谱系选择页（根）、谱首页、家谱树、人物详情（含照片灯箱）、搜索、统计分析（世代/男女/房支图表）、家族文献、关于本谱、使用手册
- 认证：手机号注册（身份校验制：姓名+父亲+母亲须匹配族谱）+ 短信验证码登录 + 密码登录 + 管理员密码登录；JWT 带谱隔离，跨谱访问 401
- 互动：提交族人信息变更（新生/结婚/去世）→ 管理员审核（通过/驳回）；「**整理变动**」后台一键把通过记录生成世系表文本 → 意见反馈 → 管理员回复
- 照片：人物页上传/更换（本人+配偶两槽位），>1MB 自动压缩，**换照自动留底**（photos_history）可查历史
- 好物：多多进宝 CPS 导购（Navbar「好物」入口 + `/mall` 页，后端转链）
- 联动：知命坊跳转（`/api/{clanId}/zmf/ticket`，需登录）
- 三端：Flutter APP 与微信小程序同族谱数据，互通

## 五·B、反爬方案 A（2026-09-05 上线）

- **http 级**（`anti-crawl.conf`）：全局限流 `general` 10r/s + burst50；大数据 JSON 限流 `datadl` 2r/s + burst10；map `$bad_bot` UA 黑名单（python-requests/scrapy/wget/Go-http-client/Java 等 → 403）、map `$bot_allow` 搜索引擎白名单
- **server 级**（`chen-zongpu`）：`if ($bad_bot = 1) { return 403; }`；图片防盗链 `valid_referers none blocked server_names;`；`location ~ ^/data/clans/[a-z]+/people\.json$` 限流；⚠️ `/uploads/` 必须用 `location ^~ /uploads/` 否则被图片正则抢占
- 验证：恶意 UA/外站 referer → 403；正常浏览器/搜索引擎 → 200

## 六、核心文件索引

| 路径 | 作用 |
|---|---|
| `src/lib/clans.ts` | 谱注册表，**加新谱 = 建数据目录 + 此处加 import** |
| `src/lib/data.ts` | 多谱数据访问层（页面统一走它） |
| `src/lib/api.ts` | 多谱 API 客户端（token 按谱隔离） |
| `src/lib/router.ts` | 纯函数（clanId 推导、图片路径） |
| `src/lib/use-clan.ts` | `useClanId()`（必须 "use client"） |
| `src/app/[clanId]/` | 13 个谱内页面（含 `/mall` 好物） |
| `backend/` | FastAPI 后端（auth/identity/db + routers：auth/changes/admin/feedback/zmf/photos/ddk；辅助模块 `shixi_export.py`、`image_utils.py`） |

## 七、待办 / 风险提醒

- **爬虫攻防**：反爬方案 A 已生效，但黑名单/限流是静态规则；后续如需更强可加 JS 挑战/动态黑洞
- **管理员密码**：生产已改默认值，刷新环境变量注意保留 `ADMIN_PASSWORD`
- **短信**：验证码目前为模拟（直接返回 dev_code），真实短信服务商待接入
- **整理变动**：后台「整理变动」生成的条目**不会自动写入母本**，核对后须人工并入 genealogy.json（见管理员手册）
- **Git**：仓库已推到 GitHub `ike333/jiapu-web`（HTTPS + PAT，推送前查 WATT Toolkit）；`.gitignore` 已排除 `backend/.env`、数据库、uploads、venv、备案截图

## 八、本地试用指南

### 启动步骤

1. **启动后端 API**（端口 8000）：
   ```powershell
   cd E:\project\JiaPu_Web\backend
   python -m uvicorn main:app --host 127.0.0.1 --port 8000
   ```
   验证：`http://127.0.0.1:8000/api/health` → `{"status":"ok"}`

2. **启动前端 dev**（端口 3000）：
   ```powershell
   cd E:\project\JiaPu_Web
   npm run dev
   ```
   就绪后显示 `Ready in X.Xs`，访问 `http://localhost:3000`

> 开发环境后端地址来自 `.env.local` 的 `NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000`；生产走 Nginx 反代 `/api` 免设置。

### 访问入口

| 路径 | 内容 |
|---|---|
| `http://localhost:3000` | 谱系选择页（目前只有陈氏） |
| `http://localhost:3000/chen` | 陈氏谱首页 |
| `http://localhost:3000/chen/family-tree` | 家谱树 |
| `http://localhost:3000/chen/stats` | 统计分析 |
| `http://localhost:3000/chen/admin` | 管理后台（管理员密码登录，见本地 `.env`） |

### 试用建议顺序

1. 先逛首页/家谱树/搜索/统计（无需登录）
2. 到「意见反馈」登录后提交一条 → 到管理后台回复验证
3. 提交一条「新生/结婚/去世」变更 → 管理后台审核 → 再到「整理变动」页签生成/标记
4. 登录页有「管理员」标签页，密码见本地 backend/.env（默认 `admin123456`）

### 试用注意事项

- **注册身份校验**：注册须填族谱内真实姓名+父亲+母亲才能通过（例：姓名 `陈安治`、父亲 `陈章学`、母亲 `马氏`），否则被拒
- **短信验证码**：模拟模式，`dev_code` 自动回填
- 仅浏览功能无需登录；提交变更、反馈、审核需登录