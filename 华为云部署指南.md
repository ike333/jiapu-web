# 本地 Shell 登录华为云部署指南

> 记录如何用自己电脑的 PowerShell 直接连华为云服务器完成部署工作，含 2026-08-26 备案整改实战全过程与踩坑记录。适用项目：多谱家谱网站（JiaPu_Web）。

---

## 一、服务器信息

| 项目 | 值 |
|---|---|
| 云厂商 | 华为云（控制台 https://console.huaweicloud.com ） |
| ECS 公网 IP | `1.94.222.136` |
| 系统 | Ubuntu 24.04（1C/1G） |
| 登录账号 | `root`，**密码认证**（本机未配 SSH 免密钥） |
| 域名 | `chenmike.cn`（备案号：陕ICP备18006511号） |
| 网站根目录 | `/var/www/chen-zongpu/`（Nginx root，整站静态导出挂这里） |
| 后端 API | FastAPI + uvicorn，监听 `127.0.0.1:8000`，Nginx 反代 `/api/` 与 `/uploads/` |

---

## 二、两种登录方式怎么选

| 方式 | 入口 | 适用场景 |
|---|---|---|
| **本地 shell（首选）** | 本机 PowerShell 执行 `ssh root@1.94.222.136` | 日常部署：既能执行命令又能传文件 |
| **CloudShell（备用）** | 华为云控制台 → ECS 实例 → 远程登录 → CloudShell 登录 | 本地 ssh 报 `Connection reset` 时的应急通道，走华为云内部网络不依赖本地网络；缺点是浏览器里传文件很不方便 |

判断口诀：**能 ssh 就用 ssh；被 reset 就开 CloudShell 排查，顺便试试换网络（如手机热点）再回本地 scp**。

---

## 三、本地 Shell 登录

```powershell
ssh root@1.94.222.136
```

- 提示输密码：root 密码（与华为云 CloudShell 登录时用的同一个）
- 首次连接提示指纹，输 `yes`
- **忘记密码**：华为云控制台 → 弹性云服务器 ECS → 找到实例 → 更多 → 重置密码（可能需重启实例生效）

退出登录：`exit`。

---

## 四、文件上传

```powershell
# 把本机文件传到服务器 /root/ 下
scp "C:\Users\chenbo\AppData\Local\Temp\opencode\out.zip" root@1.94.222.136:/root/out.zip
```

同样会提示输密码。反向下载把两个参数对调即可。

---

## 五、标准部署流程（静态前端整站更新）

### 第 1 步：本机构建前检查

- **停掉 `npm run dev`**——dev 与 build 共用 `.next` 目录并行会互相破坏
- **置空 `.env.local`** 的 API 地址，否则会被内联进 JS 产物，用户浏览器会去请求他们自己电脑的 `127.0.0.1`：

  ```
  NEXT_PUBLIC_API_BASE=
  ```

### 第 2 步：构建

```powershell
npm run build        # 静态导出 → out/
```

构建完成后**恢复 `.env.local`**（本地开发用）：`NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000`

### 第 3 步：打包 zip（⚠️ 关键步骤，勿用 Compress-Archive）

Windows PowerShell 5.1 自带的 `Compress-Archive` 生成的 zip 条目路径用**反斜杠**（如 `chen\about.html`）。Linux 的 unzip 会把它当成带 `\` 的字面单文件名，**整个目录结构塌掉**：只有根目录 `index.html` 幸存，CSS/JS/子页面全部 404，表现为"网页有内容但没有样式"。

正确做法（.NET API 手工打正斜杠路径）：

```powershell
Add-Type -AssemblyName System.IO.Compression
Add-Type -AssemblyName System.IO.Compression.FileSystem
$src = "E:\project\JiaPu_Web\out"
$dst = "$env:TEMP\opencode\out.zip"
if (Test-Path $dst) { Remove-Item $dst -Force }
$fs = [System.IO.File]::Create($dst)
$zip = New-Object System.IO.Compression.ZipArchive($fs, [System.IO.Compression.ZipArchiveMode]::Create)
Get-ChildItem -LiteralPath $src -Recurse -File | ForEach-Object {
    $rel = $_.FullName.Substring($src.Length + 1).Replace("\", "/")
    [System.IO.Compression.ZipFileExtensions]::CreateEntryFromFile($zip, $_.FullName, $rel, [System.IO.Compression.CompressionLevel]::Optimal) | Out-Null
}
$zip.Dispose(); $fs.Dispose()
```

打包后自查（可选但推荐）——反斜杠条目数必须为 0：

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$z = [System.IO.Compression.ZipFile]::OpenRead("$env:TEMP\opencode\out.zip")
"条目总数: $($z.Entries.Count)；反斜杠条目: $(($z.Entries | Where-Object { $_.FullName.Contains('\') }).Count)"
$z.Dispose()
```

### 第 4 步：上传

```powershell
scp "$env:TEMP\opencode\out.zip" root@1.94.222.136:/root/out.zip
```

### 第 5 步：服务器解压上线

```bash
ssh root@1.94.222.136     # 登入后在服务器执行：
apt-get install -y unzip >/dev/null 2>&1
rm -rf /var/www/chen-zongpu/*
unzip -o /root/out.zip -d /var/www/chen-zongpu/ >/dev/null
ls /var/www/chen-zongpu/
```

`ls` 应看到 `_next  chen  zhao  wang  index.html` 等。`rm -rf *` 只清目录内容不动 Nginx 配置，放心执行。

### 第 6 步：验证（本机即可，不必在服务器上）

```powershell
# 1) 取首页引用的 CSS 路径
$html = (Invoke-WebRequest -Uri "https://chenmike.cn/" -UseBasicParsing).Content
$css = [regex]::Match($html, '/_next/static/css/[^"]+').Value

# 2) 测 CSS 状态码，应为 200
Invoke-WebRequest -Uri "https://chenmike.cn$css" -Method Head -UseBasicParsing | Select-Object StatusCode

# 3) 抽查子页面与备案号
(Invoke-WebRequest -Uri "https://chenmike.cn/chen/" -UseBasicParsing).StatusCode
$html -match "陕ICP备18006511号"
```

浏览器再 Ctrl+F5 强刷确认样式正常。

---

## 六、故障诊断套路：「网页没样式了」

1. 本机测首页引用的 CSS 是否 404（上面第 6 步的命令）
2. **CSS 404** → 上服务器看文件在不在：
   ```bash
   ls /var/www/chen-zongpu/_next/static/css/
   ```
3. 目录不存在或为空 → 大概率是 zip 反斜杠坑（见第五章第 3 步）或解压没成功；按正确方式重打包重传即可
4. 若服务器上有文件仍 404 → 检查 Nginx root 是否指向 `/var/www/chen-zongpu`（`nginx -T | grep root`）

历史教训：2026-06 首次部署曾因**漏传 `_next/` 目录**样式全丢；2026-08-26 又因 **Compress-Archive 反斜杠**复发同症状。两者现象相同，先查服务器目录再定论。

---

## 七、CloudShell 应急用法

1. 浏览器打开 https://console.huaweicloud.com 并登录（买服务器时注册的账号，手机号+密码；忘了密码走短信找回）
2. 顶部搜索「ECS」→ 弹性云服务器 → 实例列表找到 `1.94.222.136`
3. 该行右侧「远程登录」→ 选 **CloudShell 登录** → 输 root 密码
4. 可做的事：`systemctl status sshd`、`iptables -L -n`、查 fail2ban、应急改配置——排查本地 ssh 为何被拒
5. 局限：上传文件入口隐蔽（工具栏/更多菜单里的「上传文件」），大文件部署仍以本地 scp 为准

---

## 八、后端服务（前端部署一般不动它）

- 认证/变更/反馈/照片由 uvicorn 提供，纯前端更新无需重启后端
- 需要处理时先看进程：

  ```bash
  ps aux | grep uvicorn
  ```

- 重启方式按当年部署形态（nohup 或 systemd），路径以服务器实际为准：

  ```bash
  # nohup 形态示例
  kill <PID>
  cd <服务器上的backend目录>
  nohup python -m uvicorn main:app --host 127.0.0.1 --port 8000 >/root/uvicorn.log 2>&1 &
  ```

- SQLite 数据库在 `backend/data/genealogy.db`，重要操作前先备份该文件

---

## 九、实战时间线（2026-08-26 备案整改）

| 步骤 | 结果 |
|---|---|
| 收到管局通知：网站底部中央须悬挂「陕ICP备18006511号」并链接 https://beian.miit.gov.cn | — |
| `src/components/Footer.tsx` 底部中央加备案号链接（全站组件，41 个页面全部覆盖） | ✅ |
| 置空 API_BASE → `npm run build` → 恢复 `.env.local` | ✅ |
| 第一次用 `Compress-Archive` 打包上传 | ❌ 反斜杠坑：目录结构塌掉，CSS/JS 全 404，样式全丢 |
| 本机 HEAD 测 CSS 发现 404 → 检查本地 zip 条目发现 `chen\about.html` 反斜杠路径 → 定位根因 | ✅ |
| 改用 .NET ZipArchive 正斜杠重打包，scp 重传，服务器 rm+unzip | ✅ |
| CSS 返回 200，备案号上线，浏览器强刷确认 | ✅ 整改完成 |
