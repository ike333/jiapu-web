# 陈氏宗谱网站 — 开发对话纪要

> 记录从项目启动到当前状态的所有关键对话和决策
> 最后更新：2026-06-14

---

## 第一阶段：项目启动（2026-06-12）

### 背景
用户已完成「陈氏宗谱-棕溪」Flutter App 和微信小程序，希望再做一个**网站版本**，三端互通。

### 技术选型讨论
- **框架**：Next.js 14（App Router）→ 理由：SSR/SSG 混合，SEO 友好，适合家谱类内容
- **语言**：TypeScript → 理由：族谱数据结构复杂，类型安全防 bug
- **UI**：Tailwind CSS 3 → 理由：原子化 CSS，开发效率高
- **图标**：lucide-react → 理由：轻量，替代 shadcn/ui 的图标依赖
- **图表**：recharts → 理由：统计页面柱状图
- **部署**：华为云（用户已有服务器 1.94.222.136）→ 理由：域名已备案，服务器已有

### 数据层设计
- **第一期**：JSON 文件（`src/data/genealogy.json`、`documents.json`、`photo_map.json`）
- **数据访问层**：`src/lib/data.ts`，所有页面通过此文件 API 获取数据，不直接读 JSON
- **未来迁移路线**：JSON → Prisma + SQLite → Prisma + PostgreSQL
- Prisma Schema 已预写（`prisma/schema.prisma`）

### 踩坑记录（开发阶段）

**坑1：沙箱 `npm install` 不可用**
- 现象：`npm install` 在沙箱中永远跑不完
- 原因：沙箱网络受限，npm registry 访问极慢
- 解决：用户在本机执行 `npm install`，配 `.npmrc` 使用 npmmirror 国内镜像

**坑2：`@import url()` 导致全站白屏**
- 现象：`npm run dev` 启动正常，但所有页面空白
- 原因：`globals.css` 中 `@import url('https://fonts.googleapis.com/...')` 被 PostCSS 当成本地文件去解析，整条样式表编译失败
- 解决：移除 `@import`，改为 `layout.tsx` 中 `<link>` 标签加载 Google Fonts

**坑3：配置文件格式**
- 原因：`postcss.config.ts` 用 ESM 格式，Next.js 的 PostCSS 无法正确读取
- 解决：改为 `postcss.config.js`（CommonJS `module.exports`）
- 同样：`tailwind.config.ts` → `tailwind.config.js`

**坑4：`next start` vs `next dev`**
- `next start` 在生产模式下需要 `routes-manifest.json`，但 Next.js 14 的构建产物文件名有差异
- 开发阶段直接 `next dev` 即可

---

## 第二阶段：部署上线（2026-06-13）

### 部署目标
- 服务器：华为云 ECS（IP：1.94.222.136）
- 域名：`chen.chenmike.cn`（主域名 `chenmike.cn` 已 ICP 备案）
- Web 服务器：Nginx 1.24.0 (Ubuntu)

### 部署方式
使用 `next export` 生成静态文件，通过 `scp` 命令上传到服务器。

**上传命令**（在本地 PowerShell 执行）：
```powershell
npm run build && npm run export
scp -r "E:\WorkBuddy\陈氏宗谱网站\out\*" root@1.94.222.136:/var/www/chen-zongpu/
```

### Nginx 配置
- 网站根目录：`/var/www/chen-zongpu/`
- 静态文件服务：由 Nginx 直接提供 HTML/CSS/JS

### 隐私政策部署
- 文件：`store_assets/privacy_policy.html`
- 部署地址：<ADDRESS_REMOVED>
- 在 `应用商店上架资料.md`、`项目存档.md`、`App升级指南.md` 中同步更新此地址

---

## 第三阶段：问题修复（2026-06-14）

### 问题：网站 CSS/JS 全部 404

**现象**（用户截图）：
- 导航栏变成蓝色下划线链接 + 汉堡菜单按钮
- 统计图标显示为 emoji/unicode 字符
- 整体布局混乱，像没加载 CSS

**排查过程**：
1. `curl http://chen.chenmike.cn/` → 返回 200，HTML 完整
2. `curl http://chen.chenmike.cn/_next/static/css/xxx.css` → 返回 404
3. 结论：**`_next/` 目录下的所有静态资源在服务器上缺失**

**原因**：上次部署时 `scp` 上传漏了 `_next/` 目录，只传了 HTML 文件。

**解决**：
1. 在 CloudShell 里检查服务器目录：`ls /var/www/chen-zongpu/_next/` → 为空
2. 本地重新打包：`Compress-Archive -Path "E:\WorkBuddy\陈氏宗谱网站\out\_next\*" -DestinationPath "E:\WorkBuddy\陈氏宗谱网站\_next.zip"`
3. 通过 CloudShell 上传并解压到服务器
4. 验证：`curl http://chen.chenmike.cn/` → CSS/JS 正常加载

**修复后验证**：
- 所有页面（首页、家谱树、搜索、统计、文献、关于、隐私政策）均返回 200
- 网站显示正常

---

## 第四阶段：后续讨论（2026-06-14）

### 讨论1：广告接入

**用户需求**：想了解如何在网站中接入广告。

**分析**：
- Google AdSense：大陆用户无法加载 Google 域名下的广告脚本，基本不可用
- 百度联盟：需要 ICP 备案（已有）+ 一定流量，可尝试
- 实际收益：家谱网站流量极小（每天可能就几个人），广告收益接近于零

**结论**：作为学习目的可以接入百度联盟，但实际收益可忽略。建议在网站放公众号二维码/小程序码引流，间接变现。

### 讨论2：ICP 备案

**问题**：`chen.chenmike.cn` 是否需要单独备案？

**结论**：不需要。`chenmike.cn` 主域名已备案，子域名自动包含在内。

### 讨论3：SSH 连接问题

**现象**：`ssh root@1.94.222.136` 报错 `kex_exchange_identification: read: Connection reset`

**排查**（通过 CloudShell 登录服务器）：
1. SSH 服务正常运行（`systemctl status sshd` → active）
2. 端口 22 正常监听（`ss -tlnp | grep sshd`）
3. 日志显示用户 IP `222.90.140.234` 被拒绝：`refused connect from 222.90.140.234`
4. 未安装 fail2ban
5. 可能原因：华为云安全组限制了 SSH 来源 IP

**解决建议**：
- 方案1：通过 CloudShell 上传文件（不依赖 SSH）
- 方案2：换个网络（手机热点）再试 SSH
- 方案3：在华为云控制台检查安全组入方向规则

---

## 当前状态（2026-06-14 15:00）

| 项目 | 状态 |
|------|------|
| 网站域名 | `chen.chenmike.cn` ✅ 正常访问 |
| 网站样式 | ✅ 已修复（_next/ 目录重新上传） |
| 隐私政策 | ✅ 已部署至 `chen.chenmike.cn/privacy` |
| Flutter App | V1.0 待上架华为市场，软著申请中 |
| 微信小程序 | 已上传审核 |
| 广告接入 | 待用户注册百度联盟 |
| SSH 连接 | ❌ 用户 IP 被拒绝，需用 CloudShell 上传文件 |

---

## 待办事项

- [ ] 注册百度联盟，获取广告代码
- [ ] 修复 SSH 连接问题（检查华为云安全组）
- [ ] 在网站关于页面添加公众号二维码/小程序码/华为市场下载链接
- [ ] 软著拿到后，提交华为应用市场上架
- [ ] 微信小程序审核通过后发布

---

*此文件由 AI 根据对话记录整理，最后更新：2026-06-14*
