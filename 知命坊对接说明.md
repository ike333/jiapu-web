# 知命坊对接说明

> 多谱家谱网站（JiaPu_Web）与知命坊（ZMF）联动：从家谱「知命坊详测」按钮跳转到知命坊排盘。
> 本文档说明对接的完整代码与流程，供排查、修改与在其他项目复用。

## 一、总体流程

```
浏览器（家谱 person 页）                         FastAPI 后端（家谱）                知命坊（zmf68.com）
        │                                          │                              │
        │ 1. 点击「知命坊详测」（需登录）             │                              │
        │ POST /api/{clanId}/zmf/ticket            │                              │
        │     { name, gender, birthDate,           │                              │
        │       birthTime, birthPlace }            │                              │
        ├──────────────────────────────────────────►│                              │
        │                                          │ 2. 校验登录、取用户信息          │
        │                                          │ 组装 fields + HMAC-SHA256 签名   │
        │                                          │ POST https://api.zmf68.com/api/import │
        │                                          ├──────────────────────────────►│
        │                                          │ 返回 { ticket }                │
        │                                          │ 拼跳转 URL（ticket + data + name）│
        │ ◄─────────────────────────────────────────┤                              │
        │ 3. window.open(url) 新窗口打开知命坊排盘    │                              │
```

## 二、前端代码（TypeScript）

### 2.1 出生信息解析与资格预判 —— `src/lib/birth.ts`

只有满足以下条件的命主才显示「知命坊详测」按钮：

- **出生于 1900 年 1 月 1 日以后**；
- **出生年月日齐全**（detail 中能解析出「生于 X 年 X 月 X 日」）；
- **已登录注册用户**（`getToken()` 非空）。

`parseBirth(detail)` 从族人 detail 文本中解析出生信息，支持格式：

| 格式 | 示例 | 说明 |
|---|---|---|
| 公历 | `生于公历1953年6月9日` | 可直接得到 `solarDate` 预填 |
| 农历 | `生于农历1953年九月六日` | 仅判断年份与月日齐全 |
| 民国 | `生于民国七年（1918年）腊月二十四日` | 年 = 1911 + 民国年 |
| 光绪 | `生于光绪二十八年四月十八日巳时` | 年 = 1874 + 光绪年 |
| 宣统 | `生于宣统元年腊月初三日` | 年 = 1908 + 宣统年 |
| 干支括号公历 | `生于嘉庆九年（1804年）甲子七月十七` | 用括号内公历年 |

`isZmfEligible(person)` 返回 `year >= 1900` 即认为可详测。

### 2.2 详测按钮组件 —— `src/components/ZmfButton.tsx`

点击「知命坊详测」→ 弹出表单（出生日期/时间/地点，日期可预填）→ 调后端取跳转 URL → 新窗口打开。

```tsx
const res = await fetch(`${API_BASE}/api/${clanId}/zmf/ticket`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    Authorization: `Bearer ${token}`,   // 必须登录
  },
  body: JSON.stringify({
    name, gender,          // gender: "男" | "女"
    birthDate, birthTime, birthPlace,
  }),
});
// res.data.url -> window.open(url, "_blank")
```

### 2.3 person 页接入 —— `src/app/[clanId]/person/page.tsx`

```tsx
const zmfEligible = isZmfEligible(person);
const zmfBirth = parseBirth(person.detail || "");
// ...
{zmfEligible && !!getToken() && (
  <ZmfButton name={person.name} gender={person.gender} birthDate={zmfBirth?.solarDate} />
)}
```

## 三、后端代码（FastAPI）—— `backend/routers/zmf.py`

### 3.1 接口定义

- 路径：`POST /api/{clan_id}/zmf/ticket`
- 认证：`Authorization: Bearer <token>`，且 token 的 `clan` 声明必须等于当前谱系（跨谱 401）
- 请求体：`{ name?, gender?, birthDate?, birthTime?, birthPlace? }`（均为可选，缺省用登录用户信息）

### 3.2 签名算法（与知命坊 wrangler.jsonc 的 ZONGPU_SECRET 一致）

```python
import hashlib, hmac, time

ZMF_ZONGPU_SECRET = "f9e9b4b2b49b946daacfbab977e432a4f963fa96342acd12c22b5d28cfb2b7e7"

def _hmac_hex(secret: str, message: str) -> str:
    return hmac.new(secret.encode(), message.encode(), hashlib.sha256).hexdigest()

fields = {
    "phone": user["phone"],            # 家谱用户手机号
    "name": name,                       # 族人姓名
    "password": user.get("password", ""),  # 家谱 PBKDF2 哈希，转移至知命坊
    "gender": gender,
    "ts": str(int(time.time() * 1000)),  # 毫秒时间戳
}
sig = _hmac_hex(ZMF_ZONGPU_SECRET, "&".join(
    f"{k}={fields[k]}" for k in sorted(fields)
))
payload = {**fields, "sig": sig}
```

### 3.3 调知命坊导入接口

```python
req_json = json.dumps(payload, ensure_ascii=False).encode("utf-8")
req_ = urllib.request.Request(
    "https://api.zmf68.com/api/import",
    data=req_json,
    headers={"Content-Type": "application/json"},
    method="POST",
)
# 成功返回 {"success": true, "data": {"ticket": "..."}}
```

### 3.4 跳转 URL 组装

```python
from urllib.parse import quote
# data 参数供知命坊排盘页：男_YYYY-M-D_H-M_出生地
data_param = f"{gender}_{d}_{t}_{birth_place}"   # 如 男_1953-6-9_8-30_陕西旬阳
target = f"{ZMF_PAIPAN_URL}?ticket={ticket}&data={quote(data_param)}"
if name:
    target += f"&name={quote(name)}"
```

`ZMF_PAIPAN_URL` 默认 `https://zmf68.com/paipan`。

## 四、环境变量（后端）

| 变量 | 默认值 | 说明 |
|---|---|---|
| `ZMF_ZONGPU_SECRET` | `f9e9b4b2...` | 与知命坊 wrangler.jsonc 的共享密钥 |
| `ZMF_IMPORT_URL` | `https://api.zmf68.com/api/import` | 知命坊建号/复用接口 |
| `ZMF_PAIPAN_URL` | `https://zmf68.com/paipan` | 知命坊排盘页 |

## 五、数据流与注意点

1. `data` 参数只含排盘信息（性别/日期/时间/出生地），**不含手机号与密码**。
2. `password` 字段是家谱用户的 PBKDF2 哈希，用于知命坊建号/复用同一账号，纯文本不外传。
3. 出生日期：前端预填 `solarDate`（仅公历格式能解析时）；用户可在表单中修改。
4. 资格判断只在前端（`birth.ts`）完成；后端不重复校验，但会校验登录态与谱系归属。
5. 生产环境：Nginx 已反代 `/api` → 8000，`NEXT_PUBLIC_API_BASE` 留空走同源。

## 六、验证方式

- 开发：`npm run dev` 后访问 `/chen/person?id=xxx`（找一个 1900 后出生的族人），未登录不显示按钮；登录后显示并可跳转。
- 后端：`cd backend && python -m uvicorn main:app --port 8000`，用 curl 带 token 调 `POST /api/chen/zmf/ticket` 验证返回 `url`。