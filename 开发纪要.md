# 陈氏宗谱网站 — 开发纪要

> 基于 Next.js 14 的全功能族谱网站，数据来源于「陈氏宗谱-棕溪」APP 及微信小程序。
> 已多谱化：当前为多谱基座（陈氏/赵氏/王氏三谱，见 `多谱基座改造-交接.md`、`网站现状.md`）。
> 最后更新：2026-09-05

---

## 一、项目背景

### 1.1 项目来源
「陈氏宗谱-棕溪」已有三个端：
- **Flutter App**（V1.0，待上架华为市场）
- **微信小程序**（已上传审核）
- **网站**（本项目）

三端共享同一份族谱数据（809人，14世），网站作为公开访问入口，无需安装即可使用。

### 1.2 目标用户
- 陈氏家族成员（主要）
- 对家谱文化感兴趣的研究者（次要）

### 1.3 核心需求
1. 查看完整家谱树（14世，809人）
2. 搜索族人
3. 查看人物详情（含照片）
4. 阅读族谱文献
5. 查看家族统计
6. 完全离线优先（网站部署后无需维护）

---

## 二、技术选型与理由

| 层面 | 选型 | 理由 |
|------|------|------|
| **框架** | Next.js 14 (App Router) | SSR/SSG 混合，SEO 友好；`next export` 生成纯静态文件，部署简单 |
| **语言** | TypeScript | 族谱数据结构复杂（树形 + 配偶 + 照片映射），类型安全防 bug |
| **UI** | Tailwind CSS 3 | 原子化 CSS，开发效率高；响应式设计内置 |
| **图标** | lucide-react | 轻量（~20KB），覆盖所有需要的图标；替代 shadcn/ui 的图标依赖 |
| **图表** | recharts | React 原生图表库，柱状图/饼图支持好，文档完善 |
| **部署** | 华为云 ECS（Nginx） | 域名已备案，服务器已有；静态文件部署极简 |
| **数据** | 第一期 JSON，预留 Prisma | JSON 够用且零依赖；Prisma Schema 已预写，随时可迁移 |

### 2.1 为什么不用 Vue/Nuxt？
用户熟悉 React 生态（Flutter App 用的是 Dart，但网站选择 React 系是因为 Next.js 的 SEO 和静态导出能力最强）。

### 2.2 为什么不用 Gatsby/Remix？
Next.js 的 `next export` 生成纯静态文件，部署到 Nginx 零配置；Gatsby 需要 GraphQL，过重；Remix 不支持纯静态导出。

---

## 三、数据层设计

### 3.1 当前数据结构（JSON 文件）

```
src/data/
  genealogy.json    — 树形族谱数据（5,187 行，809人，14世）
  documents.json    — 家族文献（新修谱序、公议规条、家训、二编说明、陈氏排行）
  photo_map.json    — 人物-照片映射（53 张照片）
public/images/
  person/           — 人物照片（按世代分目录）
```

### 3.2 数据访问层：`src/lib/data.ts`

所有页面**不直接读 JSON**，而是通过 `data.ts` 的 API 获取数据：

```typescript
// 页面代码
import { getAllPersons, getPersonById, searchPersons } from '@/lib/data'

// data.ts 内部实现
import genealogyData from '@/data/genealogy.json'
```

**好处**：
- 未来切换数据库（SQLite → PostgreSQL）只改 `data.ts`
- API 签名不变，页面代码零修改

### 3.3 数据库迁移路线

```
第一期：JSON 文件（当前）
  ↓
第二期：Prisma + SQLite（开发机，本地测试）
  ↓
第三期：Prisma + PostgreSQL（生产环境，改一行连接串即可切换）
```

Prisma Schema 已预写（`prisma/schema.prisma`），包含：
- `Person`（人物基本信息）
- `Spouse`（配偶关系）
- `Photo`（照片映射）
- `Document`（家族文献）
- `User`（未来用户系统）
- `EditLog`（未来修谱日志）

---

## 四、页面结构

| 路由 | 文件 | 功能 | 状态 |
|------|------|------|------|
| `/` | `src/app/page.tsx` | 首页：族谱概览、人口统计、房支入口、微信公众号 | ✅ |
| `/family-tree` | `src/app/family-tree/page.tsx` | 家谱树：逐层展开、姓名搜索、返回顶部 | ✅ |
| `/person/[id]` | `src/app/person/[id]/page.tsx` | 人物详情：基本信息、配偶、照片灯箱、子女列表、祖先面包屑 | ✅ |
| `/search` | `src/app/search/page.tsx` | 搜索：姓名模糊匹配（含配偶名），结果高亮 | ✅ |
| `/stats` | `src/app/stats/page.tsx` | 统计：世代分布、房支占比（recharts 柱状图） | ✅ |
| `/documents` | `src/app/documents/page.tsx` | 文献：家族文献列表 | ✅ |
| `/about` | `src/app/about/page.tsx` | 关于：项目说明 | ✅ |
| `/privacy` | `src/app/privacy/page.tsx` | 隐私政策（部署在服务器，非 Next.js 路由） | ✅ |

---

## 五、组件设计

| 组件 | 文件 | 说明 | 状态 |
|------|------|------|------|
| **Navbar** | `src/components/Navbar.tsx` | 全局导航栏，移动端汉堡菜单 | ✅ |
| **TreeView** | `src/components/TreeView.tsx` | 家谱树逐层展开组件 | ✅ |
| **PhotoLightbox** | `src/app/person/[id]/PhotoLightbox.tsx` | 照片灯箱（本人+配偶双栏），支持全屏缩放 | ✅ |

### 5.1 Navbar 组件
- 桌面端：水平导航栏
- 移动端：汉堡菜单 + 侧边抽屉
- 当前页面高亮

### 5.2 TreeView 组件
- 初始只显示第1世
- 点击展开按钮，逐级显示子代
- 支持搜索结果高亮

### 5.3 PhotoLightbox 组件
- 点击照片触发
- 本人照片 + 配偶照片并排显示
- 支持双指捏合缩放（移动端）
- 点击关闭或按 ESC 退出

---

## 六、类型定义

`src/lib/types.ts` — 全局类型，包括：

```typescript
interface PersonFlat {
  id: string
  name: string
  gender: 'male' | 'female'
  generation: number
  fatherId: string | null
  spouse: string | null
  children: string[]
  photo: string | null
}

interface TreeNode {
  id: string
  name: string
  gender: 'male' | 'female'
  generation: number
  children: TreeNode[]
}

interface SearchResult {
  id: string
  name: string
  gender: 'male' | 'female'
  generation: number
  matchReason: string  // 匹配原因（本人姓名/配偶姓名）
}

interface PhotoInfo {
  personId: string
  fileName: string
  path: string
}
```

---

## 七、配置文件详解

### 7.1 `next.config.js`
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // 启用静态导出
  basePath: '',       // 部署在域名根目录，无需子路径
  images: {
    unoptimized: true // 禁用 Next.js 图片优化（静态导出不支持）
  }
}

module.exports = nextConfig
```

### 7.2 `tailwind.config.js`
```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        'genealogy-blue': '#1565C0',
        'genealogy-gold': '#D4A017',
      }
    },
  },
  plugins: [],
}
```

### 7.3 `postcss.config.js`
```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

⚠️ **注意**：必须用 `.js` 后缀（CommonJS `module.exports`），不能用 `.ts`（ESM 格式），否则 Next.js 的 PostCSS 无法正确读取。

---

## 八、踩过的坑（完整版）

### 坑1：沙箱 `npm install` 不可用
- **现象**：`npm install` 在沙箱中永远跑不完（卡在 `Resolving dependencies...`）
- **原因**：沙箱网络受限，npm registry（registry.npmjs.org）访问极慢/超时
- **解决**：
  1. 用户在本机（Windows）执行 `npm install`
  2. 配置 `.npmrc` 使用 npmmirror 国内镜像：
     ```
     registry=https://registry.npmmirror.com
     ```

### 坑2：`@import url()` 导致全站白屏
- **现象**：`npm run dev` 启动正常，但所有页面空白（白屏）
- **原因**：`globals.css` 中 `@import url('https://fonts.googleapis.com/...')` 被 PostCSS 当成本地文件去解析，整条样式表编译失败
- **解决**：
  1. 移除 `globals.css` 中的 `@import url(...)`
  2. 改为在 `src/app/layout.tsx` 中用 `<link>` 标签加载 Google Fonts：
     ```tsx
     <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;700&display=swap" />
     ```

### 坑3：配置文件格式（`.ts` vs `.js`）
- **现象**：Next.js 启动时警告 `postcss.config.ts` 无法正确读取
- **原因**：`postcss.config.ts` 用 ESM 格式（`export default`），但 Next.js 的 PostCSS 期望 CommonJS（`module.exports`）
- **解决**：
  - `postcss.config.ts` → `postcss.config.js`
  - `tailwind.config.ts` → `tailwind.config.js`
  - 两个文件都用 `module.exports = { ... }` 格式

### 坑4：`next start` vs `next dev`
- **现象**：`next build && next start` 启动后访问页面报错 `routes-manifest.json not found`
- **原因**：`next start` 在生产模式下需要 `routes-manifest.json`，但 Next.js 14 的构建产物文件名有差异（或 `output: 'export'` 模式下不支持 `next start`）
- **解决**：
  - 开发阶段：`next dev`
  - 生产部署：用 `next export` 生成静态文件，由 Nginx 直接提供，不运行 Node.js 服务器

### 坑5：部署时漏传 `_next/` 目录
- **现象**：网站页面能访问（HTML 正常），但样式全丢、图标显示为 emoji、布局混乱
- **原因**：`scp` 上传时只传了 HTML 文件，漏了 `_next/` 目录（CSS/JS 静态资源）
- **排查**：
  ```bash
  curl -sI "http://chenmike.cn/_next/static/css/xxx.css"  # → 404
  ls /var/www/chen-zongpu/_next/                                    # → 为空
  ```
- **解决**：
  1. 本地重新打包：`Compress-Archive -Path "E:\WorkBuddy\陈氏宗谱网站\out\_next\*" -DestinationPath "E:\WorkBuddy\陈氏宗谱网站\_next.zip"`
  2. 通过华为云 CloudShell 上传 `_next.zip`
  3. 在 CloudShell 里解压：`unzip ~/_next.zip -d /var/www/chen-zongpu/_next/`
  4. 验证：`curl -sI "http://chenmike.cn/_next/static/css/xxx.css"` → 200

### 坑6：`.npmrc` 路径格式错误
- **现象**：npm 缓存目录被创建到 `C:\e\WorkBuddy\...`（错误位置）
- **原因**：`.npmrc` 里写了 `cache=/e/WorkBuddy/...`（Git Bash 路径格式），但 npm 是 Windows 原生程序，不认识 `/e/`，解析成了相对路径 `C:\e\`
- **解决**：
  1. 修正 `.npmrc` 路径为 Windows 格式：`cache=E:\WorkBuddy\陈氏宗谱网站\.npm-cache`
  2. 删除错误创建的 `C:\e` 目录

---

## 九、部署流程（完整版）

### 9.1 构建静态文件
```powershell
cd E:\project\JiaPu_Web
npm run build          # 构建（含静态导出，产物在 out/）
```

### 9.2 检查构建产物
```powershell
ls out/                  # 应包含：index.html, _next/, images/, person/
ls out/_next/static/    # 应包含：css/, chunks/
```

### 9.3 上传到服务器

**方法A：通过 CloudShell 上传（推荐，避免 SSH 被拒）**
1. 本地打包：`Compress-Archive` 会生成反斜杠路径（Linux unzip 目录塌掉），**须用 .NET ZipArchive 正斜杠打包**（见 `华为云部署指南.md` 第五章第3步）
2. 在华为云控制台打开 CloudShell
3. 点击「上传文件」按钮，选择 `out.zip`
4. 在 CloudShell 里解压：
   ```bash
   cd /var/www/chen-zongpu/
   rm -rf *                          # 清空旧文件
   unzip ~/out.zip -d /var/www/chen-zongpu/
   find /var/www/chen-zongpu -type d -exec chmod 755 {} +
   find /var/www/chen-zongpu -type f -exec chmod 644 {} +
   ```
   > ⚠️ 解压后必须修正权限，否则 `_next/static` 缺执行位会导致静态资源 404（2026-09-05 踩坑）

**方法B：通过 SCP（需要 SSH 可用）**
```powershell
scp -r "E:\project\JiaPu_Web\out\*" root@1.94.222.136:/var/www/chen-zongpu/
```

### 9.4 验证部署
```bash
# 在服务器上检查
ls -la /var/www/chen-zongpu/
ls -la /var/www/chen-zongpu/_next/static/css/
curl -sI "http://chenmike.cn/" | head -5
curl -sI "http://chenmike.cn/_next/static/css/xxx.css" | head -5
```

### 9.5 Nginx 配置（服务器上）
```nginx
server {
    listen 80;
    server_name chenmike.cn;

    root /var/www/chen-zongpu;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /_next/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

## 十、运行方式

### 10.1 本机开发
```bash
cd E:\project\JiaPu_Web
npm install          # 安装依赖（首次）
npm run dev          # 启动开发服务器 → http://localhost:3000/chen
```

### 10.2 生产构建
```bash
npm run build        # 构建（含静态导出，out/）
```

### 10.3 本机预览静态文件
```bash
npx serve out/      # 启动本地静态文件服务器 → http://localhost:3000
```

---

## 十一、后续计划

> ⚠️ 本节为早期规划，多数已完成。当前实际待办以 `网站现状.md`「待办/风险提醒」为准。

| 优先级 | 功能 | 状态 | 备注 |
|--------|------|------|------|
| **P0** | D3.js 吊线图/世系图可视化 | 待开发 | 家谱树可视化核心功能 |
| **P0** | 数据管理后台（在线增删改族人） | 待开发 | 需要数据库迁移 |
| **P1** | 数据库迁移（JSON → Prisma + SQLite） | Schema 已写 | 随时可启动 |
| **P1** | CSV/PDF 导出 | 待开发 | 方便族人保存 |
| **P1** | 照片墙 + 时间轴 | 待开发 | 增强展示效果 |
| **P2** | 用户系统（注册/登录/权限） | ✅ 已上线 | 2026-08-11 起 |
| **P2** | 族内留言板/修谱协作 | 待开发 | 增强互动 |
| **P2** | 全文检索（带高亮定位） | 待开发 | 当前只有姓名搜索 |
| **P3** | 修谱日志/版本对比 | 待开发 | 追踪数据变更 |
| **P3** | 广告接入 | ✅ 已上线 | 多多进宝 CPS（2026-09-05） |

---

## 十二、相关文件

| 文件 | 说明 |
|------|------|
| `E:\project\JiaPu_Web\package.json` | 项目依赖和脚本 |
| `E:\project\JiaPu_Web\next.config.js` | Next.js 配置 |
| `E:\project\JiaPu_Web\src\lib\data.ts` | 数据访问层（多谱版，2026-08-15 改造） |
| `E:\project\JiaPu_Web\src\lib\types.ts` | 全局类型定义 |
| `E:\project\JiaPu_Web\src\lib\clans.ts` | 谱注册表（多谱架构核心） |
| `E:\project\JiaPu_Web\backend\` | FastAPI 后端（auth/identity/db + routers） |
| `E:\project\JiaPu_Web\prisma\schema.prisma` | 数据库迁移预写 Schema |

---

## 十三、重要提醒

### 13.1 每次部署前必查
- [ ] `out/` 目录是否包含 `_next/static/css/` 和 `_next/static/chunks/`
- [ ] 上传后是否在服务器上验证 CSS/JS 可访问
- [ ] 是否更新了 `网站开发对话纪要.md`

### 13.2 SSH 连接问题
如果遇到 `kex_exchange_identification: read: Connection reset`：
1. 先通过华为云 CloudShell 登录服务器
2. 检查 `/etc/hosts.deny` 是否有你的 IP
3. 检查华为云安全组入方向规则是否限制了 SSH 来源 IP
4. 如果都无法解决，直接用 CloudShell 上传文件

---

## 十三、2026-08-11 功能修订记录

1. **房支归属修正**：`src/lib/data.ts` 展平逻辑改为第2代用房支名（自龙/自凤/自虎/自秀），第1代始祖无房支（显示"始祖"）；同步 `scripts/generate_people_data.py`，列表卡不再出现"陈三进房"
2. **新生儿表单**：新增「出生时间」字段（birthTime），后端 `BirthChange` 同步
3. **结婚表单**：去掉「新郎父亲 / 新娘父亲」，新增「新娘出生日期」（brideBirthDate）；后端 `MarriageChange`、admin/my-changes 显示、`export_changes_shixi.py` 同步
4. **域名**：统一为 `https://chenmike.cn/chen`（原 chen.chenmike.cn 已废弃）
5. **联系方式**：移除实体地址，改为在「陈氏宗谱」微信群联系
6. **新增意见反馈**：`feedbacks` 表 + `backend/routers/feedback.py`（`/api/feedback` 提交、`/mine` 我的、`/list` 管理员查看、`/reply/{id}` 回复）+ `src/app/feedback/page.tsx`（提交+我的反馈）+ admin 页「反馈管理」标签 + Navbar/首页入口

---

## 十四、2026-09-05 功能修订记录

1. **整理人员变动**：管理后台新增「整理变动」页签，一键把已审核变更渲染成世系表文本（不落库），核对并入母本后点「标记已处理」（写 `changes.exported_at` 防重复）。后端新 API：`GET /api/{clanId}/admin/export/pending`、`POST /api/{clanId}/admin/export/generate`、`POST /api/{clanId}/admin/export/mark`；新模块 `backend/shixi_export.py`（`changes_to_text`，`scripts/export_changes_shixi.py` 改为 import 自它防漂移）
2. **照片自动压缩**：后端 `image_utils.compress_image`（Pillow），超过 1MB 的原始照片自动转 JPEG 压缩到 ~900KB；Nginx 需 `client_max_body_size 10m` 防 413
3. **换照片自动留底**：上传更换时若该 person+slot 已有旧图，按 `photos` 表当前 filename 移入 `backend/uploads/{clanId}/prev/` 并写 `photos_history` 表；新增公开 `GET /api/{clanId}/photos/history?person_name=&slot=` 查询历史
4. **反爬方案 A（Nginx）**：http 级 `anti-crawl.conf`（限流 general 10r/s、datadl 2r/s；UA 黑名单 `$bad_bot`→403、白名单 `$bot_allow`）+ server 级（坏 UA 403、图片防盗链 `valid_referers none blocked server_names;`、people.json 限流、`/uploads/` 用 `^~` 前缀）。配置备份在服务器 `/root/*.bak_20260905`
5. **多多进宝 CPS 导购**：后端 `backend/routers/ddk.py`（`GET /api/ddk/goods?keyword=&page=&page_size=`，goods.search + promotion.url.generate 转链）、前端 `[clanId]/mall/page.tsx` + Navbar「好物」入口；凭证 PDD_CLIENT_ID/SECRET/PID 存 `backend/.env`。详见 `docs/多多进宝接入记录.md`
6. **Git 仓库**：2026-09-05 初始化并推送 GitHub `ike333/jiapu-web`（首次推送被 Push Protection 拦截，已从对话记录移除泄露 token；`.gitignore` 排除 backend/.env、数据库、uploads、venv、备案截图）

---

*最后更新：2026-09-05*
