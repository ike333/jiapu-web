# 陈氏宗谱网站 — 开发纪要

> 基于 Next.js 14 的全功能族谱网站，数据来源于「陈氏宗谱-棕溪」APP 及微信小程序。

## 基本信息

| 项 | 内容 |
|---|---|
| 项目名称 | 陈氏宗谱网站（chen-genealogy-web） |
| 工作目录 | `E:\WorkBuddy\陈氏宗谱网站\` |
| 开始日期 | 2026-06-12 |
| 数据来源 | `chen_genealogy_app`（Flutter APP）+ `chen_genealogy_wx`（微信小程序） |

---

## 技术栈

| 层面 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 14 (App Router) | SSR/SSG 混合，SEO 友好 |
| 语言 | TypeScript | 族谱数据结构复杂，类型安全防 bug |
| UI | Tailwind CSS 3 | 原子化 CSS，开发效率高 |
| 图标 | lucide-react | 轻量，替代 shadcn/ui 的图标依赖 |
| 图表 | recharts | 统计页面柱状图 |
| 部署 | Vercel / CloudStudio | 免费额度够用 |
| 数据库 | 第一期 JSON，预留 Prisma + SQLite/PostgreSQL | 参见下文「数据层设计」 |

## 数据层设计

**当前（第一期）：JSON 文件**

```
src/data/
  genealogy.json    — 树形族谱数据（5,187 行）
  documents.json    — 家族文献
  photo_map.json    — 人物-照片映射（53 张）
public/images/      — 人物照片
```

**数据访问层**：`src/lib/data.ts`

所有页面通过 `data.ts` 的 API 获取数据，不直接读 JSON。这样做的好处：
- 未来切换数据库（SQLite → PostgreSQL）只改这一个文件
- API 签名不变，页面代码零修改

**数据库迁移路线**：
1. 第一期：JSON 文件（当前）
2. 第二期：Prisma + SQLite（开发机）
3. 第三期：Prisma + PostgreSQL（生产环境，改一行连接串即可切换）

Prisma Schema 已预写（`prisma/schema.prisma`），包含 Person、Spouse、Photo、Document、User、EditLog 等表。

---

## 页面结构

| 路由 | 文件 | 功能 |
|---|---|---|
| `/` | `src/app/page.tsx` | 首页：族谱概览、人口统计、房支入口、微信公众号 |
| `/family-tree` | `src/app/family-tree/page.tsx` | 家谱树：逐层展开、姓名搜索、返回顶部 |
| `/person/[id]` | `src/app/person/[id]/page.tsx` | 人物详情：基本信息、配偶、照片灯箱、子女列表、祖先面包屑 |
| `/search` | `src/app/search/page.tsx` | 搜索：姓名模糊匹配（含配偶名），结果高亮 |
| `/stats` | `src/app/stats/page.tsx` | 统计：世代分布、房支占比（recharts 柱状图） |
| `/documents` | `src/app/documents/page.tsx` | 文献：家族文献列表 |
| `/about` | `src/app/about/page.tsx` | 关于：项目说明 |

## 组件

| 组件 | 文件 | 说明 |
|---|---|---|
| Navbar | `src/components/Navbar.tsx` | 全局导航栏 |
| TreeView | `src/components/TreeView.tsx` | 家谱树逐层展开组件 |
| PhotoLightbox | `src/app/person/[id]/PhotoLightbox.tsx` | 照片灯箱（本人+配偶双栏） |

## 类型定义

`src/lib/types.ts` — 全局类型，包括 PersonFlat、TreeNode、SearchResult、PhotoInfo 等。

---

## 踩过的坑

1. **沙箱 `npm install` 不可用**
   - 现象：`npm install` 在沙箱中永远跑不完
   - 原因：沙箱网络受限，npm registry 访问极慢
   - 解决：用户在本机执行 `npm install`，配 `.npmrc` 使用 npmmirror 国内镜像

2. **`@import url()` 导致全站白屏**
   - 现象：`npm run dev` 启动正常，但所有页面空白
   - 原因：`globals.css` 中 `@import url('https://fonts.googleapis.com/...')` 被 PostCSS 当成本地文件去解析，整条样式表编译失败
   - 解决：移除 `@import`，改为 `layout.tsx` 中 `<link>` 标签加载 Google Fonts

3. **配置文件格式**
   - 原因：`postcss.config.ts` 用 ESM 格式，Next.js 的 PostCSS 无法正确读取
   - 解决：改为 `postcss.config.js`（CommonJS `module.exports`）
   - 同样：`tailwind.config.ts` → `tailwind.config.js`

4. **`next start` vs `next dev`**
   - `next start` 在生产模式下需要 `routes-manifest.json`，但 Next.js 14 的构建产物文件名有差异
   - 开发阶段直接 `next dev` 即可

---

## 后续计划

| 优先级 | 功能 | 状态 |
|---|---|---|
| P0 | D3.js 吊线图/世系图可视化 | 待开发 |
| P0 | 数据管理后台（在线增删改族人） | 待开发 |
| P1 | 数据库迁移（JSON → Prisma + SQLite） | Schema 已写 |
| P1 | CSV/PDF 导出 | 待开发 |
| P1 | 照片墙 + 时间轴 | 待开发 |
| P2 | 用户系统（注册/登录/权限） | 待开发 |
| P2 | 族内留言板/修谱协作 | 待开发 |
| P2 | 全文检索（带高亮定位） | 待开发 |
| P3 | 修谱日志/版本对比 | 待开发 |
| P3 | 部署上线（Vercel / 华为云） | 待评估 |

---

## 运行方式

```bash
# 安装依赖
cd E:\WorkBuddy\陈氏宗谱网站
npm install

# 启动开发服务器
npm run dev
# → http://localhost:3000

# 生产构建
npm run build
npm start
```

---

*最后更新：2026-06-12*
