# 2026-08-11 改动记录

> 今日完成两件事：① 工作目录迁移（解决汉字路径问题）；② 新增「认证 + 族人信息变更记录」模块（验证码暂用模拟）。
> 2026-08-16 注：本记录所引根目录素材（`世系表.txt` 等）已移入 `material/chen/`，后续读写以 `material/` 下路径为准。

---

## 一、工作目录迁移

- 源目录：`E:\WorkBuddy\陈氏宗谱网站`（含汉字，已不再使用）
- 新目录：`E:\Project\Chen_Web`
- 复制范围：`src/`、`public/`、`prisma/`、`scripts/`、`网站连接备案/`、全部根目录配置文件与 4 篇开发文档
- 未复制（可再生）：`node_modules`、`.next`、`out/`、`.git`、缓存
- 验证：所有文件均为合法 UTF-8（无 BOM）
- 项目记忆已写入：`E:\Project\Chen_Web\AGENTS.md`

---

## 二、新增「认证 + 族人信息变更记录」模块

### 架构
静态站（Next.js 静态导出）+ 独立 FastAPI 后端 + SQLite。前端需登录后才能提交新生/结婚/去世信息，提交后由管理员审核。

### 后端（新增目录 `backend/`）

| 文件 | 说明 |
|---|---|
| `backend/main.py` | FastAPI 入口，CORS 配置，挂载路由 |
| `backend/db.py` | SQLite 数据层（自动建表） |
| `backend/auth.py` | 密码哈希(PBKDF2)、JWT、模拟短信验证码 |
| `backend/routers/auth.py` | 发送验证码/注册/登录/当前用户 |
| `backend/routers/changes.py` | 提交新生/结婚/去世 + 我的记录 |
| `backend/routers/admin.py` | 管理员审核（待审核列表/通过/驳回） |
| `backend/requirements.txt` | fastapi / uvicorn / pyjwt |
| `backend/data/genealogy.db` | SQLite 数据库（运行时自动生成，勿手改） |
| `backend/identity.py` | 族谱身份校验（姓名+父亲姓名匹配 genealogy.json） |
| `backend/data/genealogy.json` | 身份校验用族谱数据副本（族谱更新后需重新复制） |

### 前端（新增 4 个页面 + 1 个工具文件）

| 文件 | 说明 |
|---|---|
| `src/app/login/page.tsx` | 登录/注册页，模拟验证码自动填入；注册需填「姓名+父亲姓名」做族谱身份校验 |
| `src/app/submit/page.tsx` | 提交变更页（新生/结婚/去世三类表单） |
| `src/app/my-changes/page.tsx` | 我的记录页（查看提交及审核状态） |
| `src/app/admin/page.tsx` | 管理员审核页（待审核/已通过/已驳回） |
| `src/lib/api.ts` | 后端 API 客户端（含 token 存取、登录状态） |

### 前端（修改）

| 文件 | 改动 |
|---|---|
| `src/components/Navbar.tsx` | 增加登录/注册、提交变更、审核入口（桌面+移动端） |
| `src/app/page.tsx` | 首页功能导航增加「提交信息变更」卡片 |

### 配置（新增/修改）

| 文件 | 说明 |
|---|---|
| `.env.local` | 开发环境 `NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000` |
| `package.json` | `npm install` 后 package-lock.json 已更新 |

---

## 三、关键规则与坑（新项目经验）

1. 首个注册用户自动成为管理员（`backend/routers/auth.py` 中判断用户数为 0）
2. 短信验证码为模拟：`send_sms_code` 直接返回验证码，前端自动填入。接入真实短信时改 `backend/auth.py` 中 `SMS_ENABLED=1` 分支
3. **FastAPI 坑**：`authorization` 参数必须写 `authorization: str = Header(None)`，否则被当成 query 参数导致 401
4. 审核通过仅改状态，不自动写入 `genealogy.json`（需人工或后续脚本合并）
5. 开发访问地址带 basePath：`http://localhost:3000/chen/login`
6. **注册身份校验制**（2026-08-11 补充）：注册必须提供族谱中存在的姓名（支持去后缀匹配如「陈仁勇A」→「陈仁勇」），父亲姓名选填但用于二次校验；匹配失败拒绝注册（detail 提示联系管理员）
7. **母亲校验**（2026-08-11 补充）：父亲节点 detail 中提取配偶"姓"（"宋氏"→宋，"王莉"→王），注册者母亲姓须命中其一；父亲无配偶记录时跳过。**注意**：族谱用 A/B/C 后缀区分同名兄弟（如陈安治 vs 陈安治C），不能去后缀按名词索引，否则父子错配——identity.py 已用"精确名优先+去后缀回退"解决
8. **身份校验数据**：`backend/data/genealogy.json` 是 `src/data/genealogy.json` 的副本，族谱数据更新后必须重新复制一份，否则注册校验用的是旧数据
9. **注册页提示**（2026-08-11）：新增"仅浏览无需注册，注册是为了更新数据"的提示文案
10. **遇前端白屏/500**：多半是 `npm run build` 覆盖了 dev server 的 `.next` 缓存，停 dev → 删 `.next` → 重启 dev 即恢复

## 四、管理员入口改为密码进入 + 世系表导出（2026-08-11 追加）

### 1. 管理员密码登录（替代"首个注册自动 admin"）
- **变更**：注册用户一律 `role=user`，不再自动升级为 admin
- **新增接口**：`POST /api/auth/admin-login`，入参 `{password}`，密码来自环境变量 `ADMIN_PASSWORD`（默认 `admin123456`，**生产必须改**，可在 `backend/auth.py` 或启动环境变量中设置）
- **实现**：管理员 token 的 `sub` 为特殊值 `"admin"`（虚拟用户，不在 users 表）；`backend/auth.py` 新增 `resolve_user()`，`admin.py`/`changes.py`/`auth.py` 的鉴权统一走它
- **前端**：登录页新增「管理员」标签页，仅需输密码 → 进入 `/admin` 审核页
- 测试要点：管理员密码 token 可访问 `/api/admin/pending`、普通用户访问返回 403

### 2. 世系表导出程序 `scripts/export_changes_shixi.py`
- **背景**：`public/data/people.json` ← `src/data/genealogy.json` ← `世系表.txt`（家谱母本）。审核通过的变动需转成世系表格式才能并入
- **用途**：读取 `backend/data/genealogy.db` 中 `status='approved'` 的 changes，按世系表.txt 风格输出
- **用法**：
  - `python scripts/export_changes_shixi.py` → 生成 `世系表-新增条目.txt`（不碰母本）
  - `python scripts/export_changes_shixi.py --apply` → 合并进 `世系表.txt`（先自动备份 `.bak`；**注意母本当前为只读属性，写前需先取消只读**）
- **格式规则**（对照 世系表.txt）：
  - 人名行 `名字：生于…，逝于…。葬…。妻X氏，生于…。育子N：a、b、c。育女N：…。电话：…。`
  - 世系表.txt 中名字省略"陈"姓前缀（崇斌 而非 陈崇斌），apply 时自动去前缀；`.txt` 文件为 GBK 编码
  - 新生儿行按 `父世代+1` 插到对应"第N代"区块末尾；父行的「育子N」列表自动追加并更新计数（中文数字一/二/三…）
  - 结婚/去世仅生成提示文本，需人工核对后补录（配偶/逝日涉及繁体细节，不自动改）
- 测试用例：陈崇斌新增子"道小米" → 父行变 `育子二：道浩轩、道小米`，新行插入第十四代末尾

## 五、启动命令（开发环境）

```powershell
# 后端（先启动）
cd E:\Project\Chen_Web\backend
python -m uvicorn main:app --host 0.0.0.0 --port 8000

# 前端（另开终端）
cd E:\Project\Chen_Web
npm run dev
# 浏览器访问 http://localhost:3000/chen/login
```

---

*记录时间：2026-08-11 · 陈波*
